# SmartAdmin V2 底座使用规则

> **核心原则：SmartAdmin 是 V2 的正式系统底座，xsy-scm 是业务产品。**
>
> V2 不再把 SmartAdmin 当作“只读参考”。SmartAdmin 承担全部系统通用能力
> （登录、认证、用户、员工、部门、角色、菜单、权限、数据权限、日志、字典、
> 文件、统一异常、统一响应、前端 Layout 与系统页面），xsy-scm 只在其上实现
> 供应链业务域。禁止机械复制旧代码，禁止机械把 React 翻译成 Vue。
>
> **本文件为 V2 唯一有效的底座使用规则。**
> 2026-09-14 之前的“SmartAdmin 仅作只读参考、不得替换 React 技术栈”表述已全部废止，
> 详见 [`docs/architecture/2026-09-14-smartadmin-v2-迁移审计报告.md`](./docs/architecture/2026-09-14-smartadmin-v2-迁移审计报告.md)。

## 0. V2 决策基线（不可协商）

```text
1. SmartAdmin 是 V2 的正式系统底座，不是参考实现
2. 系统能力全部采用 SmartAdmin，旧 auth / system 实现不迁移
3. 只迁移 xsy-scm 供应链业务域
4. 管理后台统一 Vue3 + TypeScript
5. 小程序统一 uni-app + Vue3
6. 当前不是 SaaS，不引入 tenant_id / 多租户
7. 数据库保持 PostgreSQL，不切 MySQL
8. 业务金额一律 BigDecimal，对外定点数契约不变
9. 不引入 Spring Cloud、微服务、分布式事务、独立搜索引擎
10. 不引入数据库外键
11. V2 正式工作区固定为根目录 `xsy-scm-server/` 与 `xsy-scm-web/`；`v2/` 仅为迁移过渡目录
```

## 1. 工作区与仓库定位

### 1.1 目录划分

```text
xsy-scm/
├── xsy-scm-server/              ← V2 正式后端（代码移动目标）
├── xsy-scm-web/                 ← V2 正式后台（代码移动目标）
├── v2/                          ← 迁移过渡目录（代码移动完成后不再写入新代码）
│   ├── xsy-scm-v2-server/       ← 待移动的 SmartAdmin + SCM 后端
│   └── xsy-scm-v2-web/          ← 待移动的 SmartAdmin Vue3 前端
├── xsy-scm-miniapp/             ← LEGACY，冻结只读（后续迁 uni-app）
├── project-reference-examples/
│   └── xsy-scm/                 ← 上游源码参考（只读，用于同步与比对）
└── docs/                        ← 规格与设计文档
```

### 1.2 状态定义

| 目录 | 状态 | 允许操作 |
|---|---|---|
| `xsy-scm-server/**` | **正式** | 后端开发、重构、提交 |
| `xsy-scm-web/**` | **正式** | 前端开发、重构、提交 |
| `v2/xsy-scm-v2-server/**` / `v2/xsy-scm-v2-web/**` | **迁移过渡** | 仅用于移动前的验证与迁移 |
| `project-reference-examples/xsy-scm/**` | **上游只读** | 只读、比对、提取设计 |
| `xsy-scm-miniapp/**` | **冻结只读** | 只读，待 W6 迁移 |

### 1.3 禁止事项

- 禁止修改 `project-reference-examples/**`；
- 禁止修改上游参考目录 `project-reference-examples/xsy-scm/**`；
- 禁止修改 legacy 小程序目录 `xsy-scm-miniapp/`；
- 代码移动完成前，不要在根目录前后端创建与 `v2/` 并行的第二份实现；
- 禁止把 legacy 代码整体或机械复制进 V2 根目录工作区；
- 禁止机械把 React 组件翻译成 Vue 组件（必须按 Vue3 原生范式重写）；
- 禁止在 V2 中引入 React 或双级侧栏布局；
- 禁止为适配 legacy 而修改 SmartAdmin 核心认证体系；
- 禁止复制 legacy 的 `sys_user` / `sys_menu` / `sys_role` 等旧系统表；
- 禁止套用旧 Spring Session JDBC 方案。

## 2. 技术栈事实源

### 2.1 后端

```text
xsy-scm-server/
Java 21 + Spring Boot 3.5.4 + MyBatis-Plus 3.5.12
Sa-Token 1.44.0 + Redis（sa-token-redis-jackson）
PostgreSQL + Flyway
包根：net.lab1024.sa.base（框架） / net.lab1024.sa.admin（业务）
```

分层约定（沿用 SmartAdmin）：

```text
controller / service / manager / dao / constant
domain/entity / domain/form / domain/vo / domain/dto
```

SCM 业务模块放入：

```text
net.lab1024.sa.admin.module.scm.<domain>/
```

其中 `<domain>` ∈ `product | customer | supplier | order | purchase | inventory | mall`（`marketing` 为 DEFERRED）。

### 2.2 前端

```text
xsy-scm-web/
Vue 3 + TypeScript + Ant Design Vue + Vite + Pinia + Vue Router
权限指令 v-privilege
```

禁止引入 React、ProComponents、TanStack Query、Zustand、React Hook Form、Zod。

### 2.3 小程序

```text
目标：uni-app + Vue3
当前：xsy-scm-miniapp（Taro + React）冻结只读，W6 迁移
```

## 3. 系统能力归属

以下能力**全部采用 SmartAdmin 实现，不迁移 legacy 对应代码**：

```text
登录 / 退出登录 / 认证
Token 生命周期（Sa-Token + Redis Bearer）
用户 / 员工
部门 / 组织架构
角色 / 菜单 / 权限 / 按钮权限 / 接口权限
数据权限（数据范围）
操作日志 / 登录日志 / 异常日志
字典
文件上传下载与元数据
统一异常 / 参数校验 / 统一响应
分页 / 审计字段 / 状态枚举
验证码 / 配置管理（t_config）/ 消息通知
前端 Layout / 菜单 / Tabs / 权限指令 / 系统管理页面
```

对应 legacy 模块 `auth`、`system` 及 `common` 中的基础设施部分**不迁移**，仅作为业务语义与历史测试的参照。

### 3.1 统一响应契约（Q5）

V2 后台 API 完全采用 SmartAdmin：

```text
ResponseDTO<T>{ code, level, msg, ok, data, dataType }   // OK_CODE = 0
PageParam{ pageNum, pageSize, searchCount, sortItemList[] }
PageResult{ pageNum, pageSize, total, pages, list, emptyFlag }
```

- 不兼容 legacy React 的 `message` 字段与 `PageData`；
- SCM 业务错误语义必须保留（错误码数值见 §5.2）。

### 3.2 认证契约（Q4）

```text
Header：Authorization: Bearer <token>
token-style: simple-uuid，有效期 30 天
loginId 形如 "2:44"（userType:employeeId）
登录态存 Redis
方法级鉴权 @SaCheckPermission("scm:product:add")
administratorFlag 绕过权限校验
```

Pilot 阶段不迁 mall；商城认证在 mall 迁移阶段再决定，长期目标为统一 Sa-Token 多 userType。

## 4. 数据库规则

### 4.1 基线

```text
PostgreSQL（保持，不切 MySQL）
金额：BigDecimal + NUMERIC(18,4)
无数据库外键
逻辑删除：SCM 表保留 deleted BOOLEAN + @TableLogic + partial unique index
schema 演进：Flyway 唯一入口，迁移只能追加
```

### 4.2 SCM 保留的 PostgreSQL 特性（禁止改成 MySQL 风格）

```text
JSONB
partial unique index（部分唯一索引）
sequence
CHECK constraint
inventory append-only trigger（库存流水不可更新/删除）
identity（GENERATED BY DEFAULT AS IDENTITY）
```

### 4.3 SmartAdmin 系统表

系统表沿用 SmartAdmin 结构，但必须完成 MySQL → PostgreSQL 转换：

```text
bigint AUTO_INCREMENT → BIGINT GENERATED BY DEFAULT AS IDENTITY
tinyint(1) 0/1        → BOOLEAN
tinyint 非 0/1        → SMALLINT
int                   → INTEGER
datetime ON UPDATE    → TIMESTAMP（由填充器维护）
移除 ENGINE / CHARSET / COLLATE / ROW_FORMAT / USING BTREE / 显示宽度
UNIQUE KEY / INDEX    → 显式命名约束与索引（PG 索引名 schema 全局唯一）
```

### 4.4 数据库隔离

V2 使用独立 schema：

```text
database: xsy_scm
schema:   xsy_v2
currentSchema=xsy_v2
```

与 legacy 既有表完全隔离。

## 5. SCM 业务资产（不得因框架迁移简化）

### 5.1 必须保留

```text
@Version 乐观锁
idempotency_record 表 + Idempotency-Key 幂等
库存流水不可更新 / 不可删除（append-only）
SKU 差量同步
CustomerPriceResolver（客户价解析）
非标品实重规则（ordered quantity != final actual weight，两者都保留）
采购多次收货（一张采购单可多次收货）
DIRECT / DEFERRED putaway
```

### 5.2 错误码（Q7）

不要为了整理区间批量修改已投入使用的 SCM 错误码。已有数值先保留，例如：

```text
40921、40933、40926、40963、40970、40971
```

V2 新增错误码再规划统一 SCM 区间。必须保证错误语义与测试不丢失。

### 5.3 金额语义（额外确认项 1）

**SmartAdmin 的 `BigDecimalNullZeroSerializer`（null → 0）不得污染 SCM。**

```text
null / UNPRICED  ≠  0 元
```

- SCM 必须严格区分“无价/未定价”与“价格为零”；
- 进入 Product Pilot 前必须建立自动测试并验证通过。

### 5.4 配置承载（Q9）

采购超收容差优先复用 SmartAdmin `t_config`（key/value + 缓存 + 后台维护），
验证满足需求后直接复用，**不新建 `scm_config`**。

### 5.5 DEFERRED 范围（Q8）

```text
marketing：DEFERRED —— 不删除、不在 Product Pilot 迁移
重新评估时机：product / customer / pricing / order / purchase / inventory 主链稳定后
```

## 6. 迁移波次

```text
W0  基线：规则修订 + v2 工作区 + Clean Baseline + Java 21
        + PostgreSQL PoC/转换 + Flyway + Sa-Token + Vue3 后台 + 系统基础验证
W1  Product Pilot
W2  customer + supplier
W3  order + inventory
W4  purchase
W5  mall + marketing（marketing 视情况）
W6  小程序（uni-app Vue3）
```

当前进度：**W0 进行中**。W0 baseline 验收通过前不迁商品。

## 7. 开发工作流

### 7.1 通用能力

系统通用能力已由 SmartAdmin 提供，**不重新设计**。仅在其上做必要的 V2 适配，并遵守：

- 不修改 Sa-Token / 密码 / 验证码 / 统一响应 / 统一异常的核心实现；
- 需要扩展时以新增方式实现，不破坏上游基线结构；
- 上游基线升级时保留 `project-reference-examples` 作为比对源。

### 7.2 SCM 业务能力

```text
1. 阅读当前 Sprint 规格与 docs 中有效的业务设计
2. 阅读 legacy 对应模块，提取业务语义、字段、校验、状态机与测试用例
3. 在 `xsy-scm-server` 中按 SmartAdmin 分层与命名重新实现
4. 在 `xsy-scm-web` 中用 Vue3 + Ant Design Vue 原生范式重写页面
5. 补充与风险相称的测试（含定点数与 null 语义）
```

业务规则来源优先级：

```text
当前用户请求
→ AGENTS.md / CLAUDE.md / 本文件
→ 当前 Sprint 规格及相关 docs
→ legacy 实现（业务语义参照）
→ legacy 测试（行为契约参照）
→ SmartAdmin 框架惯例
```

### 7.3 禁止的大规模重构

除非用户明确批准并有独立迁移方案，禁止：

- 一次性替换 SmartAdmin 统一异常 / 响应 / 认证实现；
- 一次性重命名全部数据库对象；
- 一次性重写所有测试；
- 为统一表面风格破坏 SmartAdmin 上游结构；
- 把 legacy 的业务耦合带入 V2 系统层。

## 8. 供应链业务代码处理

商品、客户、供应商、订单、采购、库存属于 SCM 业务域，**以当前 Sprint 规格与 legacy 业务语义为准**，SmartAdmin 不提供替代实现。

```text
legacy 已有业务规则        → 完整迁移，语义不简化
legacy 已有测试            → 必须迁移并保持通过
legacy 已有 API 语义       → 保留（但响应体切换为 ResponseDTO）
legacy 已应用的 Flyway 迁移 → 绝不修改；V2 重新编号为独立迁移序列
legacy 领域模型            → 在 V2 中重新设计，保持业务不变量
```

## 9. 搜索参考代码的方法

应主动搜索并跟踪完整调用链，而不是只读取同名类。

```text
登录：login, token, sa-token, LoginController, LoginService
权限：permission, role, menu, RoleMenu, RoleEmployee
日志：log, operate, loginLog
字典：dict, dictionary
文件：file, upload, download, storage
组织：employee, department, dataScope
业务：legacy 中对应 domain 的 controller / service / mapper / entity / test
```

调查应覆盖入口、服务、数据访问、实体/DTO、配置、拦截器或切面、数据库脚本、前端调用与测试。

## 10. 迁移转换流程

任何迁移都不得机械复制，必须经过：

```text
理解 legacy / SmartAdmin 的设计意图
↓
去除框架专属耦合（包名、表名、DTO、Controller 命名）
↓
对照 SCM 业务规格与业务不变量
↓
适配 Java 21 + PostgreSQL + Flyway + Sa-Token
↓
适配 ResponseDTO / PageResult / 统一异常
↓
使用 Vue3 + Ant Design Vue 原生范式重写前端
↓
迁移并补充测试（含 BigDecimal null 语义、错误码语义）
```

## 11. 当前使用重点

```text
底座（直接采用 SmartAdmin）：
  登录 / 认证 / Sa-Token
  用户 / 员工 / 部门 / 角色
  菜单 / 权限 / 按钮权限 / 数据权限
  操作日志 / 登录日志 / 字典 / 文件
  统一异常 / 统一响应 / 分页 / 配置管理
  前端 Layout / 菜单 / Tabs / 权限指令 / 系统页面

业务（由 xsy-scm 规格驱动，在 V2 重新实现）：
  product / customer / supplier / order / purchase / inventory / mall
  （marketing 为 DEFERRED）
```

整体目标：

```text
以 SmartAdmin 为系统底座
+
在 V2 重新实现 SCM 供应链业务
+
保留全部业务不变量与测试
+
保持 PostgreSQL 特性与定点数契约
+
渐进完成 W0–W6 迁移波次
```
