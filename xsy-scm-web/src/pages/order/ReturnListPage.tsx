import { PlusOutlined, SearchOutlined } from '@ant-design/icons';
import type { ActionType, ProColumns } from '@ant-design/pro-components';import { ProTable } from '@ant-design/pro-components';import { Alert,Button,Input,Select,Space } from 'antd';import { useRef,useState } from 'react';import { useNavigate } from 'react-router-dom';import { fetchReturns } from '../../api/afterSales';import { AmountText } from '../../components/common/AmountText';import { PageContainer } from '../../components/common/PageContainer';import { StatusTag } from '../../components/common/StatusTag';import type { OrderReturn,ReturnStatus } from '../../types/sales';import styles from './Sales.module.css';
export function ReturnListPage(){const nav=useNavigate();const ref=useRef<ActionType>(null);const[keyword,setKeyword]=useState('');const[status,setStatus]=useState<ReturnStatus>();const[error,setError]=useState(false);const cols:ProColumns<OrderReturn>[]=[{title:'退货单号',dataIndex:'returnNo',width:200,render:(_,r)=><Button type="link" onClick={()=>nav(`/order-returns/${r.id}`)}>{r.returnNo}</Button>},{title:'原订单 ID',dataIndex:'orderId',width:130},{title:'原因',dataIndex:'reason',ellipsis:true},{title:'退货金额',dataIndex:'amount',width:150,align:'right',render:(_,r)=><AmountText value={r.amount}/>},{title:'状态',dataIndex:'status',width:110,align:'center',render:(_,r)=><StatusTag status={r.status}/>},{title:'操作',valueType:'option',width:100,render:(_,r)=><Button type="link" onClick={()=>nav(`/order-returns/${r.id}`)}>详情</Button>}];return <PageContainer><div className={styles.toolbar}><Space><Input value={keyword} prefix={<SearchOutlined/>} allowClear placeholder="退货单号" onChange={e=>setKeyword(e.target.value)}/><Select allowClear value={status} placeholder="全部状态" options={['PENDING','APPROVED','REJECTED','CANCELLED'].map(v=>({value:v,label:{PENDING:'待审核',APPROVED:'已批准',REJECTED:'已驳回',CANCELLED:'已取消'}[v]}))} onChange={setStatus}/><Button type="primary" onClick={()=>ref.current?.reload()}>查询</Button></Space><Button icon={<PlusOutlined/>} type="primary" onClick={()=>nav('/order-returns/new')}>新建退货</Button></div>{error?<Alert className={styles.error} type="error" message="退货申请加载失败" showIcon action={<Button onClick={()=>ref.current?.reload()}>重试</Button>}/>:null}<ProTable actionRef={ref} options={false} search={false} rowKey="id" columns={cols} pagination={{defaultPageSize:20,showSizeChanger:true}} request={async p=>{try{const d=await fetchReturns({page:p.current??1,pageSize:p.pageSize??20,keyword:keyword.trim()||undefined,status});setError(false);return{data:d.records,total:d.total,success:true}}catch{setError(true);return{data:[],total:0,success:true}}}}/></PageContainer>}
