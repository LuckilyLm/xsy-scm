import { ArrowLeftOutlined } from '@ant-design/icons';import { useQuery } from '@tanstack/react-query';import { Alert,Button,Descriptions,Input,Modal,Space,Spin,Table,Typography,message } from 'antd';import { useRef,useState } from 'react';import { useNavigate,useParams } from 'react-router-dom';import { approveReturn,cancelReturn,fetchReturn,rejectReturn } from '../../api/afterSales';import { ApiError } from '../../api/http';import { PageContainer } from '../../components/common/PageContainer';import { StatusTag } from '../../components/common/StatusTag';import styles from './Sales.module.css';const key=()=>`${Date.now()}-${Math.random().toString(36).slice(2)}`;
export function ReturnDetailPage(){const{id}=useParams();const returnId=Number(id);const nav=useNavigate();const[pending,setPending]=useState(false);const[error,setError]=useState<string|null>(null);const requestKey=useRef(key());const query=useQuery({queryKey:['return',returnId],queryFn:()=>fetchReturn(returnId)});const data=query.data;async function act(fn:()=>Promise<void>,text:string){if(pending)return;setPending(true);setError(null);try{await fn();message.success(text);await query.refetch();requestKey.current=key()}catch(c){setError(c instanceof ApiError&&c.status===409?'退货申请已被其他人处理，请重新加载':c instanceof Error?c.message:'操作失败，请重试');requestKey.current=key()}finally{setPending(false)}}function withReason(kind:'reject'|'cancel'){let reason='';Modal.confirm({title:kind==='reject'?'驳回退货申请':'取消退货申请',content:<Input.TextArea placeholder="请输入原因" onChange={e=>reason=e.target.value}/>,okButtonProps:{danger:true},onOk:async()=>{if(!reason.trim())throw new Error('请输入原因');await act(()=>kind==='reject'?rejectReturn(returnId,data!.version,reason.trim(),requestKey.current):cancelReturn(returnId,data!.version,reason.trim(),requestKey.current),kind==='reject'?'退货申请已驳回':'退货申请已取消')}})}if(query.isLoading)return <PageContainer><Spin/></PageContainer>;if(query.isError||!data)return <PageContainer><Alert type="error" message="退货详情加载失败" action={<Button onClick={()=>query.refetch()}>重试</Button>}/></PageContainer>;return <PageContainer><div className={styles.header}><Space><Button icon={<ArrowLeftOutlined/>} onClick={()=>nav('/order-returns')}>返回列表</Button><Typography.Title level={4} className={styles.title}>{data.returnNo}</Typography.Title><StatusTag status={data.status}/></Space>{data.status==='PENDING'?<Space><Button danger disabled={pending} onClick={()=>withReason('cancel')}>取消申请</Button><Button danger disabled={pending} onClick={()=>withReason('reject')}>驳回</Button><Button type="primary" loading={pending} disabled={pending} onClick={()=>act(()=>approveReturn(returnId,data.version,requestKey.current),'退货已批准，退款记录已生成')}>批准退货</Button></Space>:null}</div>{error?<Alert className={styles.error} type="error" message={error} showIcon action={<Button onClick={()=>query.refetch()}>重新加载</Button>}/>:null}<Descriptions bordered size="small" items={[{key:'order',label:'原订单 ID',children:<Button type="link" onClick={()=>nav(`/orders/${data.orderId}`)}>{data.orderId}</Button>},{key:'amount',label:'批准/预计金额',children:`¥ ${data.amount}`},{key:'version',label:'版本',children:data.version},{key:'reason',label:'退货原因',children:data.reason,span:3}]}/><Table style={{marginTop:16}} pagination={false} rowKey="orderItemId" dataSource={data.items} columns={[{title:'原订单行 ID',dataIndex:'orderItemId'},{title:'退货数量',dataIndex:'quantity',align:'right'}]}/></PageContainer>}
