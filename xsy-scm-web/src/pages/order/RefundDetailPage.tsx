import { ArrowLeftOutlined } from '@ant-design/icons';import { useQuery } from '@tanstack/react-query';import { Alert,Button,Descriptions,Input,Modal,Space,Spin,Typography,message } from 'antd';import { useRef,useState } from 'react';import { useNavigate,useParams } from 'react-router-dom';import { completeRefund,fetchRefund } from '../../api/afterSales';import { ApiError } from '../../api/http';import { PageContainer } from '../../components/common/PageContainer';import { StatusTag } from '../../components/common/StatusTag';import styles from './Sales.module.css';const key=()=>`${Date.now()}-${Math.random().toString(36).slice(2)}`;
export function RefundDetailPage(){const{id}=useParams();const refundId=Number(id);const nav=useNavigate();const[pending,setPending]=useState(false);const[error,setError]=useState<string|null>(null);const requestKey=useRef(key());const query=useQuery({queryKey:['refund',refundId],queryFn:()=>fetchRefund(refundId)});const data=query.data;function finish(){let externalReference=data?.externalReference??'';Modal.confirm({title:'确认完成退款',content:<Input placeholder="外部退款凭证（选填且不可重复）" onChange={e=>externalReference=e.target.value}/>,onOk:async()=>{if(pending||!data)return;setPending(true);setError(null);try{await completeRefund(refundId,{version:data.version,externalReference:externalReference.trim()||undefined},requestKey.current);message.success('退款已完成');await query.refetch()}catch(c){setError(c instanceof ApiError&&c.status===409?'退款已被处理或凭证重复，请重新加载':c instanceof Error?c.message:'操作失败');requestKey.current=key()}finally{setPending(false)}}})}if(query.isLoading)return <PageContainer><Spin/></PageContainer>;if(query.isError||!data)return <PageContainer><Alert type="error" message="退款详情加载失败" action={<Button onClick={()=>query.refetch()}>重试</Button>}/></PageContainer>;return <PageContainer><div className={styles.header}><Space><Button icon={<ArrowLeftOutlined/>} onClick={()=>nav('/order-refunds')}>返回列表</Button><Typography.Title level={4} className={styles.title}>{data.refundNo}</Typography.Title><StatusTag status={data.status}/></Space>{data.status==='PENDING'?<Button type="primary" disabled={pending} loading={pending} onClick={finish}>完成退款</Button>:null}</div>{error?<Alert className={styles.error} type="error" showIcon message={error} action={<Button onClick={()=>query.refetch()}>重新加载</Button>}/>:null}<Descriptions bordered size="small" column={2} items={[{key:'return',label:'退货单 ID',children:<Button type="link" onClick={()=>nav(`/order-returns/${data.returnId}`)}>{data.returnId}</Button>},{key:'amount',label:'退款金额',children:`¥ ${data.amount}`},{key:'external',label:'外部退款凭证',children:data.externalReference||'--'},{key:'version',label:'版本',children:data.version}]}/></PageContainer>}
