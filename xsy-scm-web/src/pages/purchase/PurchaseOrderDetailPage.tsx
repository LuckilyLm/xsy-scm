import { Descriptions,Table,Button,Space,Spin,Alert } from 'antd';
import { useQuery } from '@tanstack/react-query';
import { useNavigate,useParams } from 'react-router-dom';
import { fetchPurchaseOrder } from '../../api/purchases';
import { AUTHORITIES } from '../../auth/authorities';import { Permission } from '../../auth/Permission';
import { AmountText } from '../../components/common/AmountText';
import { PageContainer } from '../../components/common/PageContainer';
export function PurchaseOrderDetailPage(){const id=Number(useParams().id);const nav=useNavigate();const q=useQuery({queryKey:['purchase-order',id],queryFn:()=>fetchPurchaseOrder(id)});if(q.isLoading)return <Spin/>;if(q.isError||!q.data)return <Alert type="error" message="采购单加载失败" action={<Button onClick={()=>q.refetch()}>重试</Button>}/>;const o=q.data;return <PageContainer><Space direction="vertical" size="large" style={{width:'100%'}}><Descriptions bordered column={3} items={[{key:'no',label:'采购单号',children:o.orderNo},{key:'supplier',label:'供应商',children:o.supplierName},{key:'warehouse',label:'仓库',children:o.warehouseName},{key:'status',label:'状态',children:o.status},{key:'total',label:'金额',children:<AmountText value={o.totalAmount}/>} ]}/><Table rowKey="id" pagination={false} dataSource={o.items} columns={[{title:'SKU',dataIndex:'skuCode'},{title:'商品',dataIndex:'productName'},{title:'采购数量',dataIndex:'plannedQuantity',align:'right'},{title:'已收',dataIndex:'receivedQuantity',align:'right'},{title:'单价',dataIndex:'purchasePrice',align:'right'}]}/>{['SUBMITTED','PARTIALLY_RECEIVED'].includes(o.status)&&<Permission authority={AUTHORITIES.purchaseManage}><Button type="primary" onClick={()=>nav(`/purchases/orders/${o.id}/receipts`)}>进入收货</Button></Permission>}</Space></PageContainer>;}
