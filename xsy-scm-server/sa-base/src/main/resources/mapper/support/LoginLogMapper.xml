<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.base.module.support.loginlog.LoginLogDao">

    <select id="queryByPage" resultType="net.lab1024.sa.base.module.support.loginlog.domain.LoginLogVO">
        select
        *
        from t_login_log
        <where>
            <if test="query.userId != null">
                AND user_id = #{query.userId}
            </if>
            <if test="query.userType != null">
                AND user_type = #{query.userType}
            </if>
            <if test="query.ip != null and query.ip != ''">
                AND STRPOS(login_ip,#{query.ip}) > 0
            </if>
            <!-- startDate/endDate 在 LoginLogQueryForm 里是 String。
                 MySQL 会把字符串隐式转成日期，PostgreSQL 不会：
                 `date >= character varying` 直接报 operator does not exist。
                 这里显式 CAST，与 HelpDocDao / MessageMapper 的写法保持一致。 -->
            <if test="query.startDate != null and query.startDate != ''">
                AND CAST(create_time AS DATE) &gt;= CAST(#{query.startDate} AS DATE)
            </if>
            <if test="query.endDate != null and query.endDate != ''">
                AND CAST(create_time AS DATE) &lt;= CAST(#{query.endDate} AS DATE)
            </if>
            <if test="query.userName != null and query.userName != ''">
                AND STRPOS(user_name,#{query.userName}) > 0
            </if>
        </where>
        order by create_time desc
    </select>

    <select id="queryLastByUserId" resultType="net.lab1024.sa.base.module.support.loginlog.domain.LoginLogVO">
        select *
        from t_login_log
        where user_id = #{userId}
          and user_type = #{userType}
          and login_result = #{loginLogResult}
        order by login_log_id desc
        limit 1
    </select>

</mapper>