<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.admin.module.scm.product.dao.ProductUomDao">
    <resultMap id="vo" type="net.lab1024.sa.admin.module.scm.product.domain.vo.ProductUomVO" autoMapping="true">
        <id column="uom_id" property="uomId"/>
    </resultMap>
    <!-- 单位在业务表里按名称字符串记账，所以引用数按 name 匹配；
         只统计仍在使用的主数据字段，订单/采购/库存的历史快照永不复核，不计入。
         两处查询都用别名 d 引用 scm_uom，保证列表与删除复核口径同源。 -->
    <sql id="columns">d.id uom_id,d.version,d.uom_code,d.name,d.category,d.precision_scale,d.status,d.sort_order</sql>
    <sql id="referenceCount">
        (SELECT count(*) FROM product_sku s WHERE s.deleted = FALSE AND s.sale_unit = d.name)
        +
        (SELECT count(*) FROM supplier_sku k WHERE k.deleted = FALSE AND k.purchase_unit = d.name)
    </sql>
    <select id="selectWithReference" resultMap="vo">
        SELECT<include refid="columns"/>,
        <include refid="referenceCount"/>
        referenced_count
        FROM scm_uom d WHERE d.deleted=FALSE
        <if test="query.keyword != null and query.keyword != ''">AND (d.uom_code ILIKE CONCAT('%',#{query.keyword},'%')
            OR d.name ILIKE CONCAT('%',#{query.keyword},'%'))
        </if>
        <if test="query.status != null">AND d.status=#{query.status}</if>
        ORDER BY d.sort_order,d.id
    </select>
    <select id="selectVoById" resultMap="vo">
        SELECT<include refid="columns"/>,
        <include refid="referenceCount"/>
        referenced_count
        FROM scm_uom d WHERE d.deleted=FALSE AND d.id=#{id}
    </select>
    <select id="selectForUpdate" resultType="net.lab1024.sa.admin.module.scm.product.domain.entity.ProductUomEntity">
        SELECT *
        FROM scm_uom
        WHERE deleted = FALSE
          AND id = #{id} FOR UPDATE
    </select>
    <select id="selectNamesForUpdate"
            resultType="net.lab1024.sa.admin.module.scm.product.domain.entity.ProductUomEntity">
        SELECT * FROM scm_uom WHERE deleted=FALSE AND name IN
        <foreach collection="names" item="name" open="(" separator="," close=")">#{name}</foreach>
        ORDER BY id FOR UPDATE
    </select>
</mapper>
