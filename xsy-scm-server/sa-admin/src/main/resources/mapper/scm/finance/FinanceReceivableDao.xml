<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.admin.module.scm.finance.dao.FinanceReceivableDao">

    <select id="nextReceivableNo" resultType="long">
        SELECT nextval('finance_receivable_no_seq')
    </select>

    <!--
      正常应收的插入（append-only：version 从 0 起、deleted 恒 FALSE，
      由 ck_finance_receivable_append_only 在库级锁死）。

      ON CONFLICT 的列与 WHERE 谓词必须与 uk_finance_receivable_source_active 逐字一致（Q11）：
      该索引只写 deleted = FALSE（应收与应收明细的来源恒非空，与应付侧多一句 IS NOT NULL 不同），
      多写一个谓词会让冲突无法被索引仲裁。返回 0 = 这张订单已经生成过应收，
      由服务侧按「已生成」成功返回（生成器不是用户命令）。
    -->
    <insert id="insertOnConflictDoNothing"
            parameterType="net.lab1024.sa.admin.module.scm.finance.domain.entity.FinanceReceivableEntity"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO finance_receivable (receivable_no, source_type, source_id, order_id,
                                        customer_id, customer_name_snapshot, entry_type,
                                        original_receivable_id, amount, event_at, reason,
                                        version, deleted, created_at, updated_at, created_by, updated_by)
        VALUES (#{receivableNo}, #{sourceType}, #{sourceId}, #{orderId},
                #{customerId}, #{customerNameSnapshot}, #{entryType},
                #{originalReceivableId}, #{amount}, #{eventAt}, #{reason},
                0, FALSE, #{createdAt}, #{updatedAt}, #{createdBy}, #{updatedBy})
        ON CONFLICT (source_type, source_id)
            WHERE deleted = FALSE DO NOTHING
    </insert>
</mapper>
