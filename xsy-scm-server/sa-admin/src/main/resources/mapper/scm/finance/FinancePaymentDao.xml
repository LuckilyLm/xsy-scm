<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.admin.module.scm.finance.dao.FinancePaymentDao">

    <select id="nextPaymentNo" resultType="long">
        SELECT nextval('finance_payment_no_seq')
    </select>

    <!--
      正常付款的插入（append-only：version 从 0 起、deleted 恒 FALSE，由
      ck_finance_payment_append_only 在库级锁死，不给调用方留写别的值的机会）。
      NORMAL 行的 reverse_of_id / reason 必须为 NULL（ck_finance_payment_entry_pairing）。

      ON CONFLICT 的列与 WHERE 谓词必须与 uk_finance_payment_source_active 逐字一致（Q11）：
      少写谓词会命中「无索引可仲裁」而直接报错，无目标写法会把 payment_no 撞号一起吞掉。
      供应商付款的 source_id 为 NULL，根本不在这个索引里，所以 0 行只可能是「退款已付过」。
    -->
    <insert id="insertNormalOnConflictDoNothing"
            parameterType="net.lab1024.sa.admin.module.scm.finance.domain.entity.FinancePaymentEntity"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO finance_payment (payment_no, counterparty_type, counterparty_id,
                                     counterparty_name_snapshot, amount, method, paid_at,
                                     entry_type, reverse_of_id, reason,
                                     external_reference, source_type, source_id, remark,
                                     version, deleted, created_at, updated_at, created_by, updated_by)
        VALUES (#{paymentNo}, #{counterpartyType}, #{counterpartyId},
                #{counterpartyNameSnapshot}, #{amount}, #{method}, #{paidAt},
                #{entryType}, #{reverseOfId}, #{reason},
                #{externalReference}, #{sourceType}, #{sourceId}, #{remark},
                0, FALSE, #{createdAt}, #{updatedAt}, #{createdBy}, #{updatedBy})
        ON CONFLICT (source_type, source_id)
            WHERE deleted = FALSE AND source_id IS NOT NULL DO NOTHING
    </insert>
</mapper>
