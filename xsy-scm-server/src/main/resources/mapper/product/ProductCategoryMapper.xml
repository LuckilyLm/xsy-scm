<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xianshuyuan.scm.product.mapper.ProductCategoryMapper">

    <resultMap id="CategoryResultMap" type="com.xianshuyuan.scm.product.entity.ProductCategoryEntity">
        <id property="id" column="id"/>
        <result property="parentId" column="parent_id"/>
        <result property="categoryCode" column="category_code"/>
        <result property="name" column="name"/>
        <result property="level" column="level"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="status" column="status"/>
        <result property="version" column="version"/>
        <result property="deleted" column="deleted"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="createdBy" column="created_by"/>
        <result property="updatedBy" column="updated_by"/>
    </resultMap>

    <select id="selectActiveCategories" resultMap="CategoryResultMap">
        SELECT id, parent_id, category_code, name, level, sort_order, status,
               version, deleted, created_at, updated_at, created_by, updated_by
        FROM product_category
        WHERE deleted = FALSE
        ORDER BY sort_order, id
    </select>

    <select id="countActiveChildren" resultType="long">
        SELECT COUNT(*)
        FROM product_category
        WHERE parent_id = #{parentId} AND deleted = FALSE
    </select>

    <select id="countActiveProducts" resultType="long">
        SELECT COUNT(*)
        FROM product_spu
        WHERE category_id = #{categoryId} AND deleted = FALSE
    </select>
</mapper>
