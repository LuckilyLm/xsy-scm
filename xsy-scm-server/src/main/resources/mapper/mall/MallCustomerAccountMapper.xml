<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xianshuyuan.scm.mall.mapper.MallCustomerAccountMapper">

    <sql id="columns">
        id, customer_id, username, password_hash, contact_name, contact_phone, wechat_openid,
        status, last_login_at, version, deleted, created_at, updated_at, created_by, updated_by
    </sql>

    <select id="selectActiveByUsername" resultType="com.xianshuyuan.scm.mall.entity.MallCustomerAccountEntity">
        SELECT
        <include refid="columns"/>
        FROM mall_customer_account
        WHERE deleted = FALSE
        AND LOWER(username) = LOWER(#{username})
    </select>

    <select id="selectActiveByWechatOpenid" resultType="com.xianshuyuan.scm.mall.entity.MallCustomerAccountEntity">
        SELECT
        <include refid="columns"/>
        FROM mall_customer_account
        WHERE deleted = FALSE
        AND wechat_openid = #{openid}
    </select>

    <update id="updateLastLoginAt">
        UPDATE mall_customer_account
        SET last_login_at = CURRENT_TIMESTAMP,
            updated_at = CURRENT_TIMESTAMP,
            updated_by = 'MALL'
        WHERE id = #{id}
        AND deleted = FALSE
    </update>
</mapper>
