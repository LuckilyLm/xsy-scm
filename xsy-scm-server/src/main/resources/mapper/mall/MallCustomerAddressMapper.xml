<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xianshuyuan.scm.mall.mapper.MallCustomerAddressMapper">

    <sql id="columns">
        id, customer_id, receiver_name, phone, region, detail_address, is_default, status,
        version, deleted, created_at, updated_at, created_by, updated_by
    </sql>

    <select id="selectActiveByCustomerId" resultType="com.xianshuyuan.scm.mall.entity.MallCustomerAddressEntity">
        SELECT
        <include refid="columns"/>
        FROM mall_customer_address
        WHERE deleted = FALSE
        AND status = 'ENABLED'
        AND customer_id = #{customerId}
        ORDER BY is_default DESC, id
    </select>

    <select id="selectActiveByIdAndCustomer" resultType="com.xianshuyuan.scm.mall.entity.MallCustomerAddressEntity">
        SELECT
        <include refid="columns"/>
        FROM mall_customer_address
        WHERE deleted = FALSE
        AND status = 'ENABLED'
        AND id = #{id}
        AND customer_id = #{customerId}
    </select>

    <update id="clearDefault">
        UPDATE mall_customer_address
        SET is_default = FALSE,
            updated_at = CURRENT_TIMESTAMP,
            updated_by = 'MALL'
        WHERE customer_id = #{customerId}
        AND deleted = FALSE
        AND is_default = TRUE
        <if test="excludedId != null">
            AND id &lt;&gt; #{excludedId}
        </if>
    </update>

    <update id="softDeleteByIdAndCustomer">
        UPDATE mall_customer_address
        SET deleted = TRUE,
            is_default = FALSE,
            updated_at = CURRENT_TIMESTAMP,
            updated_by = 'MALL'
        WHERE id = #{id}
        AND customer_id = #{customerId}
        AND deleted = FALSE
    </update>
</mapper>
