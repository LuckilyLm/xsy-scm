<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xianshuyuan.scm.mall.mapper.MallCustomerSessionMapper">

    <sql id="columns">
        id, account_id, customer_id, token_hash, user_agent, expires_at, revoked_at, last_accessed_at,
        created_at, created_by
    </sql>

    <select id="selectActiveByTokenHash" resultType="com.xianshuyuan.scm.mall.entity.MallCustomerSessionEntity">
        SELECT
        <include refid="columns"/>
        FROM mall_customer_session
        WHERE token_hash = #{tokenHash}
        AND revoked_at IS NULL
    </select>

    <update id="revokeByTokenHash">
        UPDATE mall_customer_session
        SET revoked_at = CURRENT_TIMESTAMP
        WHERE token_hash = #{tokenHash}
        AND revoked_at IS NULL
    </update>

    <update id="revokeAllByAccountId">
        UPDATE mall_customer_session
        SET revoked_at = CURRENT_TIMESTAMP
        WHERE account_id = #{accountId}
        AND revoked_at IS NULL
    </update>

    <update id="touch">
        UPDATE mall_customer_session
        SET last_accessed_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
        AND revoked_at IS NULL
    </update>
</mapper>
