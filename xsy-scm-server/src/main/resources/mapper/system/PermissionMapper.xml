<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xianshuyuan.scm.system.mapper.PermissionMapper">
    <select id="lockActive" resultType="com.xianshuyuan.scm.system.entity.PermissionEntity">
        SELECT * FROM sys_permission WHERE id=#{id} AND deleted=FALSE FOR UPDATE
    </select>
    <select id="countCode" resultType="long">
        SELECT COUNT(*) FROM sys_permission WHERE LOWER(BTRIM(code))=#{code} AND deleted=FALSE
    </select>
    <select id="countMenuReferences" resultType="long">
        SELECT COUNT(*) FROM sys_menu WHERE LOWER(BTRIM(required_permission_code))=#{code} AND deleted=FALSE
    </select>
    <select id="countRetainedRoleGrants" resultType="long">
        SELECT COUNT(*) FROM sys_role_permission rp
        JOIN sys_role r ON r.id=rp.role_id AND r.deleted=FALSE
        WHERE rp.permission_id=#{permissionId} AND rp.deleted=FALSE
    </select>
    <update id="updatePermission">
        UPDATE sys_permission SET name=#{permission.name}, module=#{permission.module},
            resource_type=#{permission.resourceType}, status=#{permission.status}, deleted=#{permission.deleted},
            version=version+1, updated_at=CURRENT_TIMESTAMP, updated_by=#{permission.updatedBy}
        WHERE id=#{permission.id} AND version=#{permission.version} AND deleted=FALSE
    </update>
    <!-- The shared advisory lock precedes the permission row and ascending user rows.
         Disabled roles retain grants: future activation must not resurrect stale sessions. -->
    <sql id="affected">
        u.deleted=FALSE AND EXISTS (
            SELECT 1 FROM sys_user_role ur
            JOIN sys_role r ON r.id=ur.role_id AND r.deleted=FALSE
            JOIN sys_role_permission rp ON rp.role_id=r.id AND rp.deleted=FALSE
            WHERE ur.user_id=u.id AND ur.deleted=FALSE AND rp.permission_id=#{permissionId}
        )
    </sql>
    <select id="lockAffectedUsers" resultType="com.xianshuyuan.scm.system.entity.SystemUserEntity">
        SELECT u.* FROM sys_user u WHERE <include refid="affected"/> ORDER BY u.id FOR UPDATE
    </select>
    <update id="incrementAuthVersions">
        UPDATE sys_user u SET auth_version=auth_version+1 WHERE <include refid="affected"/>
    </update>
    <insert id="insertAudit">
        INSERT INTO sys_operation_log(actor_user_id,actor_name_snapshot,module,operation_code,target_type,target_id,success,before_data,after_data)
        VALUES(#{actorId},#{actor},'system',#{operation},'PERMISSION',#{id}::text,TRUE,#{before}::jsonb,#{after}::jsonb)
    </insert>
</mapper>
