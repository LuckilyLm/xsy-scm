<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xsy.scm.admin.module.business.finance.dao.VoucherEntryDao">

    <!-- 查询凭证下的分录 -->
    <select id="listByVoucherId" resultType="com.xsy.scm.admin.module.business.finance.domain.vo.VoucherEntryVO">
        SELECT
            entry_id,
            voucher_id,
            subject_code,
            subject_name,
            direction,
            amount
        FROM t_voucher_entry
        WHERE voucher_id = #{voucherId}
          AND deleted_flag = 0
        ORDER BY entry_id ASC
    </select>

    <!-- 逻辑删除凭证下的全部分录 -->
    <update id="batchUpdateDeletedByVoucherId">
        update t_voucher_entry
        set deleted_flag = 1
        WHERE voucher_id = #{voucherId}
    </update>

</mapper>
