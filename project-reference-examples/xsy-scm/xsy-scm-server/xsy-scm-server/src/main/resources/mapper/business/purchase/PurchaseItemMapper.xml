<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xsy.scm.admin.module.business.purchase.dao.PurchaseItemDao">

    <!-- 根据采购单查询明细 -->
    <select id="queryByPurchaseId" resultType="com.xsy.scm.admin.module.business.purchase.domain.entity.PurchaseItemEntity">
        SELECT *
        FROM t_purchase_item
        WHERE purchase_id = #{purchaseId}
          AND deleted_flag = 0
        ORDER BY item_id ASC
    </select>

    <!-- 分页查询采购明细 -->
    <select id="queryPage" resultType="com.xsy.scm.admin.module.business.purchase.domain.vo.PurchaseItemVO">
        SELECT
            item_id,
            purchase_id,
            product_id,
            sku_id,
            supplier_id,
            require_quantity,
            purchase_quantity,
            received_quantity,
            unit_price,
            status,
            create_time,
            update_time
        FROM t_purchase_item
        <where>
            <if test="queryForm.deletedFlag != null">
                AND deleted_flag = #{queryForm.deletedFlag}
            </if>
            <if test="queryForm.purchaseId != null">
                AND purchase_id = #{queryForm.purchaseId}
            </if>
            <if test="queryForm.productId != null">
                AND product_id = #{queryForm.productId}
            </if>
            <if test="queryForm.skuId != null">
                AND sku_id = #{queryForm.skuId}
            </if>
            <if test="queryForm.supplierId != null">
                AND supplier_id = #{queryForm.supplierId}
            </if>
            <if test="queryForm.status != null">
                AND status = #{queryForm.status}
            </if>
        </where>
        <if test="queryForm.sortItemList == null or queryForm.sortItemList.size == 0">
            ORDER BY item_id ASC
        </if>
    </select>

    <!-- 批量更新删除状态 -->
    <update id="batchUpdateDeleted">
        update t_purchase_item
        set deleted_flag = #{deletedFlag}
        WHERE item_id IN
        <foreach collection="itemIdList" open="(" close=")" separator="," item="item">
            #{item}
        </foreach>
    </update>

</mapper>
