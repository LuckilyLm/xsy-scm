<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xsy.scm.admin.module.business.supplier.dao.SupplierAccountDao">

    <!-- 分页查询供应商账号 -->
    <select id="queryPage" resultType="com.xsy.scm.admin.module.business.supplier.domain.vo.SupplierAccountVO">
        SELECT
            account_id,
            supplier_id,
            account,
            mobile,
            status,
            create_time,
            update_time
        FROM t_supplier_account
        <where>
            <if test="queryForm.deletedFlag != null">
                AND deleted_flag = #{queryForm.deletedFlag}
            </if>
            <if test="queryForm.supplierId != null">
                AND supplier_id = #{queryForm.supplierId}
            </if>
            <if test="queryForm.status != null">
                AND status = #{queryForm.status}
            </if>
            <if test="queryForm.searchWord != null and queryForm.searchWord != ''">
                AND (INSTR(account, #{queryForm.searchWord}) OR INSTR(mobile, #{queryForm.searchWord}))
            </if>
        </where>
        <if test="queryForm.sortItemList == null or queryForm.sortItemList.size == 0">
            ORDER BY account_id DESC
        </if>
    </select>

    <!-- 按登录账号查询（用于唯一性校验） -->
    <select id="getByAccount" resultType="com.xsy.scm.admin.module.business.supplier.domain.entity.SupplierAccountEntity">
        SELECT
            account_id,
            supplier_id,
            account,
            mobile,
            password,
            openid,
            login_version,
            status,
            deleted_flag
        FROM t_supplier_account
        WHERE account = #{account}
          AND deleted_flag = 0
        LIMIT 1
    </select>

    <!-- 批量更新删除状态 -->
    <update id="batchUpdateDeleted">
        update t_supplier_account
        set deleted_flag = #{deletedFlag}
        WHERE account_id IN
        <foreach collection="idList" open="(" close=")" separator="," item="item">
            #{item}
        </foreach>
    </update>

</mapper>
