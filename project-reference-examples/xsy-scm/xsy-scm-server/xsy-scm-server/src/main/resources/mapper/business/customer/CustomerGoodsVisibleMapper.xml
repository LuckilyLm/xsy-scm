<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xsy.scm.admin.module.business.customer.dao.CustomerGoodsVisibleDao">

    <!-- 分页查询客户商品可见性 -->
    <select id="queryPage" resultType="com.xsy.scm.admin.module.business.customer.domain.vo.CustomerGoodsVisibleVO">
        SELECT
            id,
            customer_id,
            product_id,
            visible_type,
            create_time,
            update_time
        FROM t_customer_goods_visible
        <where>
            <if test="queryForm.deletedFlag != null">
                AND deleted_flag = #{queryForm.deletedFlag}
            </if>
            <if test="queryForm.customerId != null">
                AND customer_id = #{queryForm.customerId}
            </if>
            <if test="queryForm.productId != null">
                AND product_id = #{queryForm.productId}
            </if>
            <if test="queryForm.visibleType != null">
                AND visible_type = #{queryForm.visibleType}
            </if>
        </where>
        <if test="queryForm.sortItemList == null or queryForm.sortItemList.size == 0">
            ORDER BY id DESC
        </if>
    </select>

    <!-- 批量更新删除状态 -->
    <update id="batchUpdateDeleted">
        update t_customer_goods_visible
        set deleted_flag = #{deletedFlag}
        WHERE id IN
        <foreach collection="idList" open="(" close=")" separator="," item="item">
            #{item}
        </foreach>
    </update>

</mapper>
