<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xsy.scm.admin.module.business.finance.dao.FinanceVoucherDao">

    <!-- 分页查询会计凭证 -->
    <select id="queryPage" resultType="com.xsy.scm.admin.module.business.finance.domain.vo.FinanceVoucherVO">
        SELECT
            voucher_id,
            voucher_no,
            voucher_date,
            voucher_type,
            biz_type,
            biz_id,
            total_debit,
            total_credit,
            sync_status,
            external_no,
            status,
            create_time,
            update_time
        FROM t_finance_voucher
        <where>
            <if test="queryForm.deletedFlag != null">
                AND deleted_flag = #{queryForm.deletedFlag}
            </if>
            <if test="queryForm.voucherType != null">
                AND voucher_type = #{queryForm.voucherType}
            </if>
            <if test="queryForm.syncStatus != null">
                AND sync_status = #{queryForm.syncStatus}
            </if>
            <if test="queryForm.status != null">
                AND status = #{queryForm.status}
            </if>
            <if test="queryForm.searchWord != null and queryForm.searchWord != ''">
                AND (INSTR(voucher_no, #{queryForm.searchWord}) OR INSTR(external_no, #{queryForm.searchWord}))
            </if>
        </where>
        <if test="queryForm.sortItemList == null or queryForm.sortItemList.size == 0">
            ORDER BY voucher_id DESC
        </if>
    </select>

    <!-- 统计同一业务单已生成的凭证数（幂等校验） -->
    <select id="countByBiz" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM t_finance_voucher
        WHERE deleted_flag = 0
        <if test="bizType != null">
            AND biz_type = #{bizType}
        </if>
        <if test="bizId != null">
            AND biz_id = #{bizId}
        </if>
    </select>

    <!-- 批量更新删除状态 -->
    <update id="batchUpdateDeleted">
        update t_finance_voucher
        set deleted_flag = #{deletedFlag}
        WHERE voucher_id IN
        <foreach collection="idList" open="(" close=")" separator="," item="item">
            #{item}
        </foreach>
    </update>

</mapper>
