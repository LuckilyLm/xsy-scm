<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xsy.scm.admin.module.business.order.dao.SaleOrderItemDao">

    <!-- 根据订单查询明细 -->
    <select id="queryByOrderId" resultType="com.xsy.scm.admin.module.business.order.domain.entity.SaleOrderItemEntity">
        SELECT *
        FROM t_order_item
        WHERE order_id = #{orderId}
          AND deleted_flag = 0
        ORDER BY item_id ASC
    </select>

    <!-- 分页查询订单明细 -->
    <select id="queryPage" resultType="com.xsy.scm.admin.module.business.order.domain.vo.SaleOrderItemVO">
        SELECT
            item_id,
            order_id,
            product_id,
            sku_id,
            quantity,
            snapshot_price,
            price_type,
            actual_weight,
            actual_quantity,
            item_amount,
            status,
            create_time,
            update_time
        FROM t_order_item
        <where>
            <if test="queryForm.deletedFlag != null">
                AND deleted_flag = #{queryForm.deletedFlag}
            </if>
            <if test="queryForm.orderId != null">
                AND order_id = #{queryForm.orderId}
            </if>
            <if test="queryForm.productId != null">
                AND product_id = #{queryForm.productId}
            </if>
            <if test="queryForm.skuId != null">
                AND sku_id = #{queryForm.skuId}
            </if>
            <if test="queryForm.status != null">
                AND status = #{queryForm.status}
            </if>
        </where>
        <if test="queryForm.sortItemList == null or queryForm.sortItemList.size == 0">
            ORDER BY item_id ASC
        </if>
    </select>

    <!-- 批量更新删除状态 -->
    <update id="batchUpdateDeleted">
        update t_order_item
        set deleted_flag = #{deletedFlag}
        WHERE item_id IN
        <foreach collection="itemIdList" open="(" close=")" separator="," item="item">
            #{item}
        </foreach>
    </update>

    <!-- 统计订单明细核算金额合计（不含税） -->
    <select id="sumItemAmount" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(item_amount), 0)
        FROM t_order_item
        WHERE order_id = #{orderId}
          AND deleted_flag = 0
    </select>

</mapper>
