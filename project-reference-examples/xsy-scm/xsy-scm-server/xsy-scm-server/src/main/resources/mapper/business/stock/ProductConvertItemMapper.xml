<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xsy.scm.admin.module.business.stock.dao.ProductConvertItemDao">

    <!-- 查询转换单下的明细 -->
    <select id="listByConvertId" resultType="com.xsy.scm.admin.module.business.stock.domain.vo.ProductConvertItemVO">
        SELECT
            item_id,
            convert_id,
            source_product_id,
            source_sku_id,
            source_quantity,
            source_weight,
            target_product_id,
            target_sku_id,
            target_quantity,
            target_weight,
            target_unit_price,
            target_amount
        FROM t_product_convert_item
        WHERE convert_id = #{convertId}
          AND deleted_flag = 0
        ORDER BY item_id ASC
    </select>

    <!-- 逻辑删除转换单下的全部明细 -->
    <update id="batchUpdateDeletedByConvertId">
        update t_product_convert_item
        set deleted_flag = 1
        WHERE convert_id = #{convertId}
    </update>

</mapper>
