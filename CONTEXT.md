# 鲜蔬源智链领域词汇

本文件统一鲜蔬源供应链业务中的核心用语。它只定义业务概念，不描述数据库、接口或代码实现。

## 商品

**商品主体（SPU）**：
一组共享名称、分类和描述的商品信息，例如“红富士苹果”。它本身不是订单、库存或称重直接引用的交易单位。
_Avoid_: 商品记录、产品模板

**商品规格（SKU）**：
商品主体下可独立销售、定价、采购、入库和称重的最小业务单位，例如“红富士苹果-按斤散装”。每个商品主体至少有一个商品规格。
_Avoid_: 规格商品、商品明细

**默认商品规格**：
商品主体下用于列表摘要和无显式规格选择场景的唯一商品规格。
_Avoid_: 主规格、基础规格

**标品**：
数量和结算口径在销售前已经确定的商品规格，例如整箱饮料或固定重量礼盒。
_Avoid_: 固定商品

**非标品**：
最终结算数量可能由收货或分拣称重确定的商品规格，例如按实际重量销售的蔬菜。
_Avoid_: 散装商品、称重商品

**规格快照**：
商品规格所包含的销售属性及其值的完整组合，例如“包装=礼盒，重量=5斤”。订单保存快照后，商品档案后续变化不影响历史单据阅读。
_Avoid_: 规格字符串、属性备注

## 客户与定价

**客户类型**：
用于分类客户档案的基础资料。客户只能引用启用的客户类型。
_Avoid_: 客户分组、结算组

**SKU 可见策略**：
决定客户可在销售订单中选择哪些商品规格的规则。`ALL_ENABLED` 表示全部已启用且已上架的 SKU；`ALLOWLIST` 表示只允许客户白名单中同时可销售的 SKU。
_Avoid_: 商品权限、客户价格范围

**客户协议价**：
在指定半开有效区间 `[生效时间, 失效时间)` 内，为某一客户和 SKU 约定的销售单价。相同客户与 SKU 的有效区间不得重叠。
_Avoid_: 客户折扣、永久价

**市场价**：
SKU 档案中的默认销售价格。当指定时点没有有效客户协议价时，作为订单价格的回退来源。
_Avoid_: 成本价、结算价

**锁定价格**：
订单提交时经服务端重新解析、校验并固定在订单行上的价格。客户协议价或市场价后续变化不会改变已提交订单的锁定价格。
_Avoid_: 当前价格、前端报价

**人工改价**：
仅在订单草稿阶段，以必填原因明确覆盖自动解析价格的行为。人工改价仍须保留来源和审计记录。
_Avoid_: 临时折扣、直接改金额

## 销售订单与结算

**销售订单**：
面向一个客户创建的销售业务单据。其状态依次为草稿（`DRAFT`）、待确认（`PENDING`）、已确认（`CONFIRMED`），草稿或待确认状态也可取消（`CANCELLED`）。
_Avoid_: 出库单、收款单

**订购数量**：
客户下单时填写并在订单全生命周期中保留的数量，不因实际称重或结算而被覆盖。
_Avoid_: 最终数量

**实际数量**：
用于最终结算的数量。标品提交时等于订购数量；非标品在订单待确认阶段由操作人录入或修正。
_Avoid_: 订购数量、退货数量

**订购金额**：
按订购数量和草稿/锁定单价计算的订单行或订单合计，用于表达下单口径。
_Avoid_: 实收金额

**结算金额**：
按实际数量与锁定价格计算的订单行或订单合计，统一保留 4 位小数并采用 `HALF_UP` 舍入。
_Avoid_: 订购金额、支付金额

**补单**：
来源标记为 `SUPPLEMENT` 的销售订单，必须填写补单原因，可关联同一客户的一张已确认原订单，并遵循普通订单的定价、审核和结算流程。
_Avoid_: 原单修改、无审计加单

## 退货与退款

**退货单**：
针对已确认销售订单申请退回部分或全部实际数量的单据，状态为待审批（`PENDING`）、已批准（`APPROVED`）、已拒绝（`REJECTED`）或已取消（`CANCELLED`）。
_Avoid_: 库存入库单

**可退数量**：
原订单行实际数量扣除待审批占用数量和已批准数量后的余额。待审批退货同样占用额度，累计申请不得超出原实际数量。
_Avoid_: 订购余量

**退款单**：
退货批准时按原订单锁定价格自动生成的资金记录，状态为待处理（`PENDING`）或已完成（`COMPLETED`）。当前阶段只记录退款状态和可选外部参考号，不执行真实支付。
_Avoid_: 退货单、支付交易

## 审计与并发

**乐观锁版本**：
随可变业务记录传递的版本号，用于检测并拒绝基于过期数据的更新。
_Avoid_: 时间戳锁

**幂等键**：
业务命令携带的唯一请求标识。相同命令、键和请求内容重放原结果；相同键对应不同内容时视为冲突。
_Avoid_: 单据编号、数据库主键

**操作日志**：
追加写入的订单业务变更记录，包含操作类型、操作人以及结构化的变更前后数据，不因主数据变化而删除或改写。
_Avoid_: 应用运行日志
