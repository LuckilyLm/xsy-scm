# xsy-scm 团队技术提升方案（提案 · 执行中）

> **执行状态（2026-09-18 补齐）：**下文保留原始提案，当前实现采用 Surefire 显式包含 IT；平台验证入口共享 `tools/verify.py`，TS 历史基线恢复到 `xsy-scm-web/quality/`。采购审计快照、审计字段及分配服务已拆分，行装配保留在分配服务内，共享请求行上下文；未继续拆收货服务或扩展业务域。另修复编辑新增行时分配缺少行 ID 的独立缺陷。具体执行结果与剩余条件见 `docs/progress.md` 和 `CONTRIBUTING.md`；下文的工具路径、行数、待确认问题是提案时快照。


> 原提案状态：Stage 0 执行中、Track C 待确认；当前状态以上方执行说明与 `docs/progress.md` 为准。
> 日期：2026-09-18
> 范围：结构治理（Track C）+ 代码评审体系（Track B）
> 介入方式：先出方案，确认后再动手

---

## 零、执行记录（2026-09-18）

按「先出方案再动手」的约定，方案交付后经确认进入执行。以下是本次实际做的改动、实测结果，以及新发现的问题。

### 已完成的改动

| 文件 | 改动 | 目的 |
| --- | --- | --- |
| `xsy-scm-server/pom.xml` | 新增 `maven-surefire-plugin` 配置，`<includes>` 显式列出四个默认模式 + `**/*IT.java` | 让 `mvn test` 真正执行 38 个集成测试类 |
| `xsy-scm-server/.../db/migration/V15__scm_purchase.sql` | 恢复被误改的注释头 | 修复 Flyway 校验和（详见下） |
| `xsy-scm-server/.../db/migration/V19__scm_inventory.sql` | 恢复被误改的注释头（2 行） | 同上 |
| `xsy-scm-server/.../db/migration/V20__scm_inventory_permissions.sql` | 恢复被误改的注释头 | 同上 |
| `.github/pull_request_template.md` | 新建 | Track B：PR 模板 |
| `CONTRIBUTING.md` | 新建 | Track B：评审清单 / 坏味道表 / 轮值机制 |
| `verify.sh` / `verify.ps1` | 新建 | Stage 0.2：一键验证入口 |

**未改动任何生产业务代码。** 唯一的构建配置改动是 surefire；唯一的迁移改动是把三个文件恢复到 `d7dbea3` 之前的内容（git blob 哈希逐一比对一致）。

### 实测基线（修复后，全绿）

```
mvn -pl sa-admin -am test
[INFO] Tests run: 566, Failures: 0, Errors: 0, Skipped: 5
[INFO] BUILD SUCCESS
[INFO] Total time: 02:29 min
```

| 指标 | 修复前 | 修复后 |
| --- | --- | --- |
| 执行测试数 | 363 | **566**（+203） |
| 执行的测试类 | 40 个 `*Test` | 78 个（40 `*Test` + **38 `*IT`**） |
| 集成测试 | **0 个执行** | **38 个类全部执行** |
| 基类误执行 | — | 0（`*ITBase` / `PgITPaths` / `Sm4EncryptCli` 均未被当作测试） |
| 跳过 | 0 | 5（全部来自 `F0FileStorageCloudIT`，需 MinIO，既有情况） |

`566 − 363 = 203`，与工作区记忆里 W6-1 验收记录的「PG IT 203」**完全吻合**——说明这批集成测试本身就是好的，只是从来没有被构建执行过。

### 执行中发现并修复的问题：Flyway 校验和失效

这是本次评审最有价值的发现，也是原方案没有预见到的。

**现象**：集成测试虽然被 surefire 发现，但全部失败于

```
FlywayValidateException: Validate failed: Migrations have failed validation
Migration checksum mismatch for migration version 15
Migration checksum mismatch for migration version 19
Migration checksum mismatch for migration version 20
```

**根因**：提交 `d7dbea3「docs(repo): consolidate project documentation」`（一个标题为「文档收敛」的提交）在删除 `docs/architecture/*` 之后，顺手「修复」了三个迁移文件里指向那些已删文档的注释链接：

```diff
--- a/.../V19__scm_inventory.sql
--- See docs/architecture/2026-09-18-w6-inventory-approval.md (Q1-Q13) and
--- docs/architecture/2026-09-18-w6-inventory-target-design.md.
+-- See docs/progress.md and docs/decisions.md for the current W6 scope.
```

**Flyway 对文件全文（含注释）计算校验和**，因此改注释 = 改校验和 = 校验失败 = ApplicationContext 无法启动 = **全部 38 个集成测试类无法运行**。

**修法**：恢复原始注释内容（用 `git show d7dbea3^:<path>` 逐字节比对确认一致）。选这个方案而不是 `flyway repair` 的理由：

- 改动是纯注释、零语义影响，恢复后所有既有数据库立即重新通过校验，**不需要对任何数据库执行操作**
- `flyway repair` 会重写 `flyway_schema_history` 的校验和记录，把「内容漂移」这一事实抹掉；而本项目已经为此专门建了 `verify_w*_legacy.py` 冻结哈希的机制，恢复原状更符合既有纪律
- 代价是三条注释重新指向已删除的文档路径。**这是正确的取舍**：迁移不可变性优先于文档链接新鲜度。这些链接的正确处置方式是在 `docs/decisions.md` 里记一条说明，而**不是**回头改迁移文件

**这条教训值得写进评审清单**：迁移的「不可变」边界不应按版本号硬编码（`AGENTS.md` 写的是「V1–V18 不可变」，而 V19/V20 当时已经应用到了开发库），而应按**「是否已被任何仍然存在的数据库应用过」**来判断。只要被应用过，一个字符都不能再动——包括注释、包括修链接。

### 一次被自己纠正的误判（保留记录，供团队参考）

排查过程中我曾用 `grep -c $'\r'` 判断行尾，得到「`deploy/minio/bootstrap.sh` 和三个迁移文件都是 CRLF」，并据此怀疑行尾是 Flyway 校验失败的成因、以及 `bootstrap.sh` 挂进 Linux 容器会执行失败。

用 `git ls-files --eol` 和 CR 字节计数复核后**推翻了这两个结论**：

```
deploy/minio/bootstrap.sh                CR字节=0   （LF，无问题）
V15__scm_purchase.sql                    CR字节=0   （LF，无问题）
V6__scm_product.sql                      CR字节=107 （CRLF，.gitattributes 明确要求）
```

`git ls-files --eol` 对 `bootstrap.sh` 显示 `i/lf w/lf`，与 `.gitattributes` 的意图一致。**行尾没有问题，`.gitattributes` 正在正确工作。**

记录这一段的用意：`grep -c $'\r'` 在 Git Bash 下给出的是假结果，**不要用它判断行尾**；权威方法是 `git ls-files --eol` 或 `tr -dc '\r' < file | wc -c`。差一点就把一个不存在的 bug 写进报告。

### 仍需你确认的事项

1. **本次改动是否提交？** 目前全部停留在工作区（`git status` 可见），未做任何 commit。
2. **`docs/progress.md` / `AGENTS.md` 是否同步更新？** 两者都写着 W6-1「待验证」、`AGENTS.md` 还写着「V21 has NOT been executed or tested」。现在基线已跑通，这些描述已经过期。按项目规则这类记录应追加到 `docs/progress.md`。
3. **`tools/` 是否纳入版本管理？** 实测发现它**不能整体入库**——`tools/gen_v3_migration.py` 里硬编码了超级管理员口令（`Xsy@2026!Admin`、`1024ok`）和加盐规则，`tools/verify_system_schema_diff.py`、`tools/w1/w2_e2e_accounts.py` 里硬编码了本机路径 `D:\PostgreSQL\bin\psql.exe`。需要先做脱敏拆分（把凭据改为环境变量、把 psql 路径改为可配置），再决定哪些脚本入库。
4. **TS 棘轮基线放哪里？** 它必须离开 `docs/`，否则下次文档收敛还会被删——这正是它这次失效的原因。
5. **Track C 结构治理是否开始？** 前置条件（绿色基线）现在已经满足。

---

### Track C Step 1：抽审计快照工厂（已完成并验证）

| 项 | 结果 |
| --- | --- |
| 新增 | `manager/PurchaseOrderAuditSnapshotFactory.java`（约 90 行，三个纯静态方法） |
| `PurchaseOrderService` | **805 → 750 行**（−67/+10），移除 `stateSnapshot` / `orderSnapshot` / `itemSnapshots` |
| 顺带清理 | 两个因此变为死导入的 import（`PurchaseOrderAllocationVO` / `PurchaseOrderItemVO`） |
| 测试改动 | **零**（这是行为等价的判据） |
| 验证结果 | `Tests run: 566, Failures: 0, Errors: 0, Skipped: 5`，78 个类 —— **与基线逐字一致** |

**为什么新建类而不是并入 `PurchaseSnapshotFactory`**：后者是 292 行的**实体快照矩阵**工厂（把主数据冻结进实体字段），而这三个方法是**审计载荷**装配（把命令前后状态序列化成 JSONB）。两者都叫「快照」但变更原因不同，合在一起会让 292 行的类继续膨胀。

**命名说明**：类名里特意加了 `Audit` 以区别于 `PurchaseSnapshotFactory`——两个名字太接近会读错。

### Track C Step 2：发现范围远超预期，暂不执行

原计划是「消灭三份复制粘贴的 `stamp`」。实际排查后发现：

```
stamp 定义共 13 份，散落在 10 个 service：
  PurchaseOrderService      ×3   PurchaseReceiptService    ×3
  CustomerService           ×1   CustomerTypeService       ×1
  SalesOrderService         ×1   OrderReturnService        ×1
  ProductSpuService         ×1   SupplierService           ×1
  WarehouseService          ×1
```

而且**实体没有共同基类**——`PurchaseOrderItemEntity`、`PurchaseOrderEntity` 等各自独立声明
`createdAt` / `updatedAt` / `createdBy` / `updatedBy` / `version` / `deleted` 字段。

要真正消除重复，需要：

1. 在 `scm/common/` 新增审计接口（声明 4 个审计 setter，Lombok 生成的 setter 天然满足）
2. 让涉及的 **13 个实体** `implements` 该接口（接口不影响 MyBatis 映射，风险低）
3. 新增 `ScmEntityStamper`，把 13 处收敛成 1 处

**为什么暂不执行**：这是一个**跨 6 个业务域**的改动，把采购单上帝类的重构和全模块审计约定统一捆在一起，会让变更难以评审和回滚——正是本方案自己的 PR 模板要拦的事。建议作为独立的、可单独评审的改动推进。

**顺带发现的两处不一致**（建议一并处理）：

- `SalesOrderService` 与 `OrderReturnService` 把整个 stamp 压成一行，与其它 11 处的展开写法不一致
- `PurchaseOrderItemEntity` 里 `@Version private Integer version=0;`、`@TableLogic(...) private Boolean deleted=false;`
  **已经在字段声明处给了默认值**，因此 `stamp(..., creating)` 里的 `setVersion(0)` / `setDeleted(false)`
  对新建实体是冗余的。**但这是语义判断，不是纯提取**，需要单独验证后再动——不要在重构里顺手删。

### Track C Step 3：设计已就绪，待批准

`PurchaseOrderAllocationService`（约 210 行）——这是收益最大的一刀：

| 迁出内容 | 行数 |
| --- | --- |
| `validateAllocations` / `recomputeDemands` / `applyAllocationChanges` / `insertAllocations` / `releaseAllocations` | ~127 |
| `lockDemands` / `loadAllocations` / `requestedDemandIds` / `totals` | ~37 |
| 内部类 `RequestedRow` + `materialize` | ~46 |

**预期**：`PurchaseOrderService` 750 → **~540 行**。

**必须守住的约束**（这三条决定成败）：

1. **锁序不能变**。`lockDemands` 是 P12 锁序的第 1 层（`create` 时需求必须先锁），抽走后必须成为新类的显式契约方法，且两个类的类注释都要保留锁序说明。
2. **事务边界不能变**。`@Transactional` 只留在编排层 `PurchaseOrderService`；新类**不带**事务注解，否则传播行为会变。
3. **`RequestedRow` 与 `materialize` 必须同批迁移**。原计划把 Step 3（分配对账）和 Step 4（行装配）分开，但 `RequestedRow` 被两者共享——`materialize` 负责构造、`validateAllocations` 负责填充 `allocations`。分开做会留下一个跨类的可变中间态，比分批更危险。

---

## 一、结论摘要

这份评审只读不改，全部结论都来自对当前工作区的实测。四条核心判断：

1. **你们的代码写得比机制好。** 代码层面零 `catch (Exception)`、零 `printStackTrace`、零 `TODO`，注释在解释「为什么」而不是「做什么」，有真并发死锁用例（`ScmInventoryConcurrencyIT`）。**缺的不是能力，是让能力可复现、可传承的机制。**

2. **验证本身没问题，问题是验证不是一个动作，而是一套需要有人记得全流程的仪式。** `mvn test` 只跑 363 个单测并报 `BUILD SUCCESS`，38 个 `*IT.java`（7,198 行）它一个都不碰；IT 得靠另一条命令单独跑（W6-1 验收时跑过 203 个）。**每一步都做对了，但没有任何东西保证下一步不会被忘掉。**

3. **已经有过「忘了跑」的真实代价。** `docs/progress.md` 里 W6-1 的后续修复（含 V21）至今写着「尚未重新运行测试」。这不是态度问题，是流程没有兜底。

4. **结构治理不能现在就开始。** 没有可复现的绿色基线就重构 `PurchaseOrderService`，你无法区分「我改坏了」和「本来就坏的」。所以 **Stage 0 是硬前提**，排在重构之前。

---

## 二、评审依据（实测数据）

| 项 | 实测值 | 来源 |
| --- | --- | --- |
| 后端 Java 文件 | 963 | `find` 统计 |
| 主代码行数 | 51,315 | 排除 `src/test` |
| 测试文件 / 行数 | 85 / 13,914（测试:主 ≈ **0.27 : 1**） | |
| 测试类构成 | `*Test.java` 41 个 / **`*IT.java` 38 个（7,198 行）** | 按后缀统计 |
| 前端源文件 / 行数 | 407 / 63,794 | 含 `province-city-district.ts` 17,546 行 |
| SCM 业务模块 | purchase 71 / order 71 / pricing 46 / product 41 / customer 33 / supplier 27 / inventory 17 / warehouse 12 | |
| 提交数 / 作者数 | 97 / **1**（巴士因子 = 1） | `git shortlog -sn` |
| `mvn test` 实测 | **363 通过 / BUILD SUCCESS / 2m26s** | 见 3.1 |
| 静态分析插件 | **0**（pom 仅 compiler + spring-boot） | grep pom |
| CI 配置 | **0**（无 `.github/workflows`、无 Jenkinsfile、无根级脚本入口） | |
| 本地 PG 容器 | 运行中（healthy，127.0.0.1:15432） | `docker ps` |
| 本地 MinIO 容器 | **未运行**（9000 未监听） | `netstat` |

---

## 三、P0 发现：验证不在任何自动门禁里

### 3.1 实测：`mvn test` 静默跳过全部集成测试

```
mvn -pl sa-admin -am test -DskipTests=false

[INFO] Results:
[INFO] Tests run: 363, Failures: 0, Errors: 0, Skipped: 0
[INFO] BUILD SUCCESS
[INFO] Total time:  02:26 min
```

原因是配置层面的必然结果，不是偶发：

- 三个 pom（根 / `sa-admin` / `sa-base`）**没有任何 surefire 或 failsafe 插件配置**
- 根 pom **没有 `<parent>`**（用 `dependencyManagement` + `spring-boot-dependencies`，不是 `spring-boot-starter-parent`）
- 于是 Maven 使用 surefire 默认包含模式：`**/Test*.java` `**/*Test.java` `**/*Tests.java` `**/*TestCase.java`

`PurchaseOrderServiceIT`、`ScmInventoryConcurrencyIT` 这类名字**一个都不匹配**；failsafe 插件不存在，所以 `mvn verify` 也不会跑它们。

注意输出里的 `Skipped: 0`——**不是「被跳过并报告」，是根本没被发现**。Maven 从不提示它没跑这 38 个类。

### 3.2 重要修正：他们跑过 IT，只是靠人工

工作区记忆 `2026-09-18.md` 记录了 W6-1 验收时的实际验证结果：

```text
后端单元    363 / 0 / 0 / 0
PG IT       203 / 0 / 0 / 5（5 skip = F0FileStorageCloudIT，需 MinIO，既有）
W6 专项     18 IT + 12 单测
前端        lint 0 error；unit 63/63；build OK；TS 棘轮 SCM 零错误区 0
Playwright  e2e/scm-inventory.spec.ts 6/6；W5 的 scm-purchase 9/9
```

**所以准确的说法不是「从未执行」，而是：**

> 验证是一套 6 步的人工仪式（单测 / IT / W6 专项 / 前端 4 项 / Playwright），每一步都做对了，但**没有任何单条命令能跑完它，也没有任何东西在提交时自动触发它**。跑不跑、跑全没跑全，取决于操作者记不记得。

这个区别很关键：它说明团队的执行力没问题，**问题在流程缺少兜底**。所以 Stage 0 的目标不是「发现测试」，而是**把已经手工做对的事情固化成一条命令**。

### 3.3 代价已经出现过两次

**代价一：真实缺陷漏到很后面。** 同一份记忆里记着：

> V19 Step 3 引用了不存在的列 `m.unit`（应为 `m.unit_snapshot`），由新 IT 首次运行抓出。

IT 确实能抓出真 bug——**但只在有人跑它的时候**。

**代价二：交付状态长期挂账。** `docs/progress.md` 至今写着：

> W6-1 后续修复（含 V21）提交后尚未重新运行测试、构建、迁移或浏览器验证。

以及 `AGENTS.md`：

> V21 has NOT been executed or tested in this review.

**代码已经交付，但没有任何机制能证明它是好的。**

### 3.4 附带损失：前端类型门禁也已失效

`tools/ts_baseline_ratchet.py check` 硬依赖 `docs/quality/ts-baseline.json`。W6-1 验收时这条门禁是好的（记忆里记着「TS 棘轮 SCM 零错误区 0」），但该文件在提交 `d7dbea3「docs(repo): consolidate project documentation」` 里被当作文档删除了：

```
$ git log --oneline --all -- docs/quality/ts-baseline.json
d7dbea3 docs(repo): consolidate project documentation     ← 删除
5fc9a7f refactor(repo): promote V2 workspaces to repository root  ← 建立
```

现在跑 check 只会得到：

```
ERROR: baseline not found at .../docs/quality/ts-baseline.json; run `capture` first.
```

**根因值得单独记一笔**：文档收敛规则要求 `docs/` 只保留少量记录，而门禁的**数据文件**恰好放在 `docs/` 下——规则被执行的时候，把门禁的输入一起清掉了。**门禁数据不能和文档共用目录。**

---

## 四、Stage 0：把人工仪式固化成一个动作（硬前提）

**目标**：在动任何生产代码之前，先让「跑一遍验证」变成一条命令、一个可信结论。

### 0.1 修 Maven 测试发现（约 10 行配置）

在根 pom 的 `<build><plugins>` 里二选一：

- **方案 A（推荐）**：引入 `maven-failsafe-plugin`，把 `*IT.java` 绑定到 `integration-test` / `verify` 阶段，`mvn verify` 全跑。
  理由：IT 依赖真实 PG 和 MinIO，与单测生命周期分离，`mvn test` 保持秒级快速反馈。
- **方案 B（最小改动）**：给 surefire 显式配置 `<includes>` 加入 `**/*IT.java`，`mvn test` 全跑。

### 0.2 补一个一键验证入口

仓库目前**没有任何脚本入口**。建议加根级 `verify` 脚本（`.sh` + `.ps1` 各一份，与 `deploy/` 的双平台风格一致），把 3.2 里那 6 步串起来：

```
1. 确保 docker compose -f deploy/postgres/compose.yaml up -d 健康
2. 确保 docker compose -f deploy/minio/compose.yaml    up -d 健康   ← 当前没起
3. mvn verify                        （单测 + IT）
4. cd xsy-scm-web && npm run typecheck && npm run lint && npm run test
5. ts_baseline_ratchet.py check      （需先恢复基线，见 0.4）
6. npx playwright test
```

### 0.3 跑通并冻结基线

第一次跑必然有失败或环境依赖问题。**这一步的产出不是「全绿」，而是「一份写下来的、已知的失败清单」**——明确哪些是环境导致（如 MinIO 未起导致的 5 个 skip）、哪些是真实缺陷（如 V21 未验证）。这份清单就是后续所有改动的对照物。

### 0.4 恢复前端类型门禁的基线

重新 `capture` 一次基线，但**放到不会被文档收敛规则扫到的位置**（例如 `xsy-scm-web/quality/ts-baseline.json` 或根级 `quality/`），并同步修改 `ts_baseline_ratchet.py` 的 `BASELINE_PATH`。

### 0.5 验收标准

- 一条命令能跑完全部验证
- 输出里能看到 `Tests run: N`，且 N 覆盖到 `*IT` 类
- 失败项有明确分类（环境 / 真实缺陷）
- 基线结果记入 `docs/progress.md`

> **为什么这不算「扩大范围」**：结构治理的全部安全性来自「重构前后测试结果一致」。没有 Stage 0，后面每一步重构都是在没有安全网的情况下盲改。

---

## 五、Track C：结构治理

### 5.1 诊断：`PurchaseOrderService`（805 行 / 28 个方法）

它现在同时承担 5 种职责：

| 职责 | 方法 | 约占行数 | 归属判断 |
| --- | --- | --- | --- |
| 命令编排 | `create` `update` `submit` `cancel` `shortClose` `delete` `batchDelete` | ~300 | **应保留** |
| 分配对账 | `validateAllocations` `recomputeDemands` `applyAllocationChanges` `insertAllocations` `releaseAllocations` | ~150 | 可独立 |
| 行装配 | `materialize` + 内部类 `RequestedRow` | ~50 | 可独立 |
| 审计快照 | `stateSnapshot` `orderSnapshot` `itemSnapshots` | ~55 | 纯序列化，零风险可抽 |
| 通用工具 | `stamp`×3 `version` `lockOrder` `save` `totals` `lockDemands` `loadAllocations` | ~120 | 含三份复制粘贴 |

**`PurchaseReceiptService`（610 行）**问题更集中：`confirm()` 单方法 **163 行**（第 205–368 行），内部串了：幂等认领 → 4 层锁序 → 容差配置读取 → 逐行数量计算 → 累计 → 对账写入 → 入库事实收集 → 库存契约调用。

`stamp(...)` 有三个重载，`PurchaseOrderEntity` / `PurchaseOrderItemEntity` / `PurchaseDemandAllocationEntity` 各一份，逻辑几乎相同（差异只在是否设置 `version` / `deleted`）——典型的复制粘贴，将来加审计字段必然漏改其中一个。

### 5.2 拆分方案：4 步，每步独立可验证

| 步骤 | 动作 | 抽出内容 | 风险 | 预期 |
| --- | --- | --- | --- | --- |
| **Step 1** | 抽 `PurchaseOrderSnapshotFactory` | `stateSnapshot` / `orderSnapshot` / `itemSnapshots`（~55 行纯函数，无状态无依赖） | 极低 | 805 → ~750 |
| **Step 2** | 抽 `ScmEntityStamper`，泛型化三份 `stamp` | 消灭重复代码 | 低 | 805 → ~720 |
| **Step 3** | 抽 `PurchaseOrderAllocationService` | 分配对账全家桶 + `lockDemands` + `totals` + `loadAllocations`（~230 行） | **中（核心）** | 805 → ~450 |
| **Step 4** | 抽 `PurchaseOrderItemAssembler` | `materialize` + `RequestedRow`（~50 行） | 中 | 805 → **~200** |

`PurchaseReceiptService` 的 `confirm()` 随后按同样思路拆为：`ReceiptLineQuantityCalculator`（已有雏形）+ `ReceiptReconciliationWriter` + `InboundFactCollector`，编排留在原方法内。

**Step 3 是收益最大也最需要小心的一刀**：`recomputeDemands` 承载「需求侧回落」这条业务不变量（不释放会让需求永久卡在 `ALLOCATED`，与 §4.4「三态可回落」直接矛盾）。抽走后，这条不变量必须显式写进新类的契约注释。

### 5.3 每步的验证方式

- 每步结束后跑：`PurchaseOrderServiceIT` + `PurchaseDemandAllocationIT` + `PurchaseOrderAllocationChangeSetTest` + `PurchaseOrderItemChangeSetTest` + `PurchaseReceiptServiceIT` + `PurchaseReceiptReconciliationIT`
- **Step 1 / Step 2 属于纯提取，必须做到「零测试改动」**——测试一行都不用改，就是行为等价的最强证明
- Step 3 / Step 4 如果被迫要改测试，**说明边界切错了，回退重切**，不要通过改测试来让重构通过

### 5.4 风险与应对

| 风险 | 应对 |
| --- | --- |
| 抽 `AllocationService` 破坏 P12 锁序 | 锁序是**跨类不变量**。抽取时把 `lockDemands` 提升为显式契约方法，并在两个类的类注释里同时保留锁序说明（`create` / `update` / `cancel` / `delete` 四种顺序不同） |
| 事务边界被改坏 | **所有 `@Transactional` 只留在编排层（`*Service`）**，被抽出的 `*Factory` / `*Assembler` / `*Calculator` 一律不带事务注解。这条要写进评审清单 |
| 静态方法抽走后调用点遗漏 | 优先实例方法 + 构造注入，而不是 `static` 工具类，让编译器兜底 |
| 基线不绿 | Stage 0 解决 |

---

## 六、Track B：代码评审体系

### 6.1 评审清单（针对本项目真实风险定制）

通用清单对你们没用——你们的风险不是「命名不规范」，是「锁序写反」「迁移改了历史版本」「幂等漏了」。所以清单直接对着你们已有的不变量写：

**写入路径**

- [ ] 新增写命令是否走 `IdempotencyService.claim` / `complete` / `replay`？
- [ ] 幂等 scope 是否唯一且含实体 id（如 `"PURCHASE_ORDER_SUBMIT:" + form.getId()`）？
- [ ] 乐观锁：是否校验 version，且 DAO 返回行数 `!= 1` 时抛 `VERSION_CONFLICT`？

**并发与锁序**

- [ ] 是否遵守 P12 锁序？本次改动有没有让两个方向相反的加锁顺序形成环？
- [ ] 批量操作是否**按 id 升序**处理（如 `batchDelete` 的 `sorted(comparing(...::getId))`）？
- [ ] 涉及 `FOR UPDATE` 的查询是否一次性按升序锁完，而不是循环内逐条锁？

**数据库**

- [ ] 是否新增了外键？（**项目禁止**，关系完整性由服务层 + 唯一约束 + CHECK 保证）
- [ ] 是否修改了 V1–V18？（**不可变**）
- [ ] 新迁移版本号是否**唯一且连续**？选号前是否 `git fetch` 并确认远端 main 是本地 HEAD 的祖先？
- [ ] `ON CONFLICT (...) WHERE ...` 的冲突目标是否与部分唯一索引**逐字一致**（Q11）？
- [ ] 金额/数量字段是否 `NUMERIC` 且精度足够？比较是否用 `compareTo` 而非 `equals`？

**领域不变量**

- [ ] 状态迁移是否走状态机（`PurchaseOrderStateMachine`），而不是裸写 `setStatus`？
- [ ] 库存：是否遵守 Q13 单位不变量？流水是否只增不改（Q7，DAO 只声明 insert + select，冲销用新反向流水）？
- [ ] 需求侧 `allocated_quantity` / `status` 是否在**旧 ∪ 新并集**上重算（漏掉旧集合会导致需求永久卡在 `ALLOCATED`）？

**架构与安全**

- [ ] `@Transactional` 是否只在编排层？被抽出的 `*Factory` / `*Calculator` 有没有误加事务注解？
- [ ] 是否触碰 F0-DEBT-01 未解决的路径？（`FileKeyVoSerializer` → `FileService.getFileList()` 无逐用户权限过滤）
- [ ] 业务附件是否走了正确的 fileKey 前缀策略与读取守卫？

**测试**

- [ ] 新增行为是否有对应 IT？
- [ ] 本次是否**改了测试来让重构通过**？纯提取类改动不允许改测试
- [ ] **IT 是否真的跑了？**（注意：`mvn test` 不跑 `*IT`，Stage 0 之前必须显式确认）

### 6.2 PR 模板（草稿）

```markdown
## 改了什么
<!-- 一句话说清行为变化 -->

## 涉及的不变量
<!-- 勾选本次触碰到的，并在下方说明如何保证 -->
- [ ] 幂等（IdempotencyService）
- [ ] 锁序 P12
- [ ] 迁移不可变性 / 版本号唯一连续
- [ ] 无外键原则
- [ ] 库存 Q13 单位不变量 / Q7 只增账本
- [ ] 事务边界（@Transactional 只在编排层）
- [ ] F0 文件访问权限

## 验证证据
<!-- 粘贴实际输出，不要写「已验证」 -->
- 执行的命令：
- 实际结果（粘贴 Tests run / BUILD 结果）：
- 未覆盖到的部分：

## 风险与回滚
```

关键设计：**「验证证据」要求粘贴实际输出，不接受「已验证」三个字**。这是把 `docs/progress.md` 里「尚未重新运行测试」这种状态从流程上堵掉——不是为了追责，是为了让「没跑」这件事在评审时就被看见。

### 6.3 坏味道对照表（用你们自己的代码举例）

| 坏味道 | 本仓库实例 | 判定阈值 |
| --- | --- | --- |
| 上帝类 | `PurchaseOrderService` 805 行 / 28 方法 | 单类 > 400 行需说明理由 |
| 长方法 | `PurchaseReceiptService.confirm()` 163 行 | 单方法 > 60 行需拆分或说明 |
| 复制粘贴 | `stamp(...)` 三个重载 | 出现第 2 份相似实现即提取 |
| 职责混杂 | 快照序列化与事务编排同处一类 | 一个类只应有一个变更原因 |
| 门禁数据混入文档目录 | `docs/quality/ts-baseline.json` 被当文档删除 | 数据文件与文档分目录 |
| 大文件混入源码 | `province-city-district.ts` 17,546 行 | 数据文件与逻辑分离 |

### 6.4 评审轮值机制（解决巴士因子 = 1）

97 次提交全部来自一位作者，意味着所有设计判断（锁序、幂等、精度口径、Q11 匹配、三态可回落）都只存在于一个人的脑子里。机制建议：

1. **每次改动必须有第二人评审**，评审人轮值，不固定为同一人
2. **评审人要在 PR 里复述一遍改动的业务意图**——复述不出来说明还没理解，不能点通过
3. **每周抽一个已合并的 PR 做回溯评审**，把发现的问题沉淀进 6.3 的对照表
4. **关键不变量指定「第二责任人」**：锁序、幂等、库存账本、迁移策略各指定一人，职责是能在原负责人缺席时解释并维护它

---

## 七、排期建议

```
Stage 0  绿色基线                        ← 硬前提，先做
  ├─ 0.1 修 Maven 测试发现（failsafe 或 surefire include）
  ├─ 0.2 加一键验证入口（把 6 步仪式固化）
  ├─ 0.3 跑通 + 冻结基线 + 记录已知失败
  └─ 0.4 恢复 TS 棘轮基线（放到不会被文档规则扫到的位置）
Step 1   抽 PurchaseOrderSnapshotFactory   （零测试改动）
Step 2   抽 ScmEntityStamper               （零测试改动）
Step 3   抽 PurchaseOrderAllocationService （核心，谨慎）
Step 4   抽 PurchaseOrderItemAssembler
Track B  评审清单 + PR 模板                ← 可与 Stage 0 并行，无需等待
Track B  轮值机制 + 第二责任人              ← 需要团队参与
```

Track B 的清单和模板不依赖任何代码改动，**可以和 Stage 0 同时推进**。

---

## 八、待确认的决策点

1. **Stage 0 的 Maven 方案选 A（failsafe，`mvn verify` 跑 IT）还是 B（surefire 加 include，`mvn test` 跑 IT）？** 我建议 A。
2. **Stage 0 是否允许我修改根 pom？** 这是本方案唯一涉及构建配置的改动。
3. **MinIO 容器当前没起**，`F0FileStorageCloudIT` 会 skip（既有情况）。是先起来，还是把这批 IT 记为「已知环境依赖」？
4. **TS 棘轮基线放哪里？** 它必须离开 `docs/`，否则下次文档收敛还会被删。
5. **重构的评审人是谁？** 结构治理每一步都需要一个能判断「行为是否等价」的第二人。
6. **是否把 `tools/` 和 `.agents/` 纳入版本管理？** 它们目前都在 `.gitignore` 里，意味着 19 个校验脚本和 7 个技能只存在于一台机器上——Stage 0 的「一键验证入口」如果调用 `tools/` 下的脚本，别人 clone 下来会直接失败。**这一条会决定 Stage 0 能否真正落地。**
