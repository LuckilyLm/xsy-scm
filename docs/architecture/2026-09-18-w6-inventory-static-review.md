# W6-1 静态复核、修复与遗留事项

日期：2026-09-18。范围：基线 `main@7a94a41` 上未提交的 W6-1、收货接线、时间编解码、前端页面及本地 PostgreSQL 配置。

用户要求：检查问题，可修复则修复，不能处理的记录，整理后提交远程；**不进行测试**。
本次只读源码、既有测试和报告，并进行 Git 差异、版本与文件检查；未运行单元测试、集成测试、浏览器测试、lint、类型检查、构建、服务启动或数据库迁移。已有报告中的通过数不算本次证据。

本次修复状态统一为 **FUNCTIONALLY IMPLEMENTED BUT UNVERIFIED**。

## 已修复

| 编号 | 问题及影响 | 修复与边界 |
| --- | --- | --- |
| R1 / P1 | V19 的 `CHECK (deleted = FALSE)` 只能拒绝软删除；修改 `unit_cost`、同时修改数量与期末值、物理删除仍能通过，违反 Q7，不再能保证账本可信 | 新增 `V21__scm_inventory_movement_append_only.sql`，语句级触发器拒绝 UPDATE / DELETE / TRUNCATE；保留 INSERT。未改 V1–V20，未应用 V21。拥有更改表结构权限的维护者仍可移除触发器，此约束不替代数据库权限管理 |
| R2 / P2 | `InventoryCommandService.postPurchaseInbound` 只靠调用约定加入事务；直接调用会让余额首建、流水插入、余额累加分别自动提交，失败时可留下半笔入库 | 写入前强制检查 `isActualTransactionActive()`；没有事务立即失败，不新开事务，现有 confirm 同事务路径保持不变 |
| R3 / P2 | 两张库存页用普通 `const columns` 接收 `TableOperator` 的 `v-model` 回写；组件初始化读取已存列配置、用户保存列设置都会发出替换数组事件 | 改为 `ref<TableColumnsType<...>>`，使数组替换可响应且不向常量赋值 |
| R4 / P2 | 收货单列表只在 setup 读取 `receiptNo`；SmartAdmin 按 route.name 缓存，后续来源链接可能显示上一次单号，旧状态/采购单筛选也会遮住目标单据 | 监听路由与来源单号，仅在本页面激活路由时处理；来源跳转清掉冲突筛选并重查第一页 |
| R5 / P2 | 新时间反序列化器对 JSON 对象/数组读取到 null 后当作无值；展示格式默认 SMART 解析可把不存在的日期纠正成另一日 | 仅允许字符串/null，展示输入使用 `uuuu` + STRICT；保留合法展示格式、ISO-8601 与空值语义 |
| R6 / P2 | 原报告 §5.1 的配置类型问题：默认语言/主题被推导为 string，reset 用任意 string 索引配置对象 | 给默认配置明确的 `AppConfig` 类型，Pinia reset 使用 `$patch`，设置表单使用 `Object.assign`；不重采或放宽类型基线，类型检查结果尚待验证 |
| R7 / P2 | PostgreSQL 操作说明声称删掉认证变量即可开启密码认证，但 compose 的回退值仍是 trust；可能让维护者误以为已开启认证 | 文档改为显式 `POSTGRES_HOST_AUTH_METHOD=scram-sha-256`，说明只对新卷初始化生效及已有卷处理；未更改运行中容器、认证或数据 |
| R8 / P2 | README、文档索引和底座规则仍写 W6 未开始；原验收总括“全部通过”与类型错误/全量 E2E 失败记录矛盾 | 同步入口与 W6-1 状态，在原验收报告和目标设计顶部增加复核说明，保留历史记录而不改写当次结果 |

## 既有测试源码的同步（未运行）

- `ScmInventoryMigrationIT` 原来只测软删除，补上普通字段更新、数量改写、DELETE、TRUNCATE 拒绝断言。
- `ScmPurchaseMigrationIT` 的迁移版本清单扩展到 V21。
- 原 backfill 用例通过删除真实流水构造历史，在 V21 下会被正确拒绝。`ScmW6PgITBase.eraseMovementsFor` 改用连接内临时账本副本，保留 CHECK/索引并让 V19 回放 SQL 使用副本；不关闭真实表触发器、不删除真实流水。该适配也未运行。

## 未解决 / 后续处理

| 编号 | 事项 | 原因与下一步 |
| --- | --- | --- |
| O1 | V21、事务防护、时间解析、表格列设置、路由复用和类型修复尚未运行验证 | 遵守本次禁止测试要求。之后获准验证时优先覆盖账本改删拒绝、收货原子回滚、backfill 临时表隔离、非法时间输入、两页列设置及复用收货页跳转 |
| O2 | 仓库没有启停写入路径（W5 G1）；库存筛选依赖 `scm:warehouse:query`，来源跳转依赖采购收货查询权限 | 静态确认独立权限依赖存在；本次未扩展权限、引入角色或新增启停业务。须裁决角色权限组合及仓库生命周期，不能仅给库存权限便假定整条筛选/溯源链可用 |
| O3 | F0-DEBT-01：业务 VO 附件展开缺少逐用户权限过滤 | 既有绑定债务，非本次库存域改动；引入非管理员业务角色之前必须处理业务权限、归属/关系和 FileService 读取 |
| O4 | 全量 E2E/MinIO/大数据与多角色验证缺口 | 原报告记录全量 E2E 22 failed、7 skipped、9 did not run，另有 MinIO 条件缺口；本次未重跑，不重新认定故障根因或宣称全部通过 |
| O5 | E2E 依赖未分发的本地 `tools/` 脚本 | 新 `scm-inventory.spec.ts` 直接调用被 Git 忽略的 `tools/w6_e2e_accounts.py`，新 clone 不能独立执行；原 W1–W5 夹具同有本机依赖。保持仓库 tools 不分发纪律，另行决定可分发夹具或外部账号注入方案 |
| O6 | 40921 在多个域枚举重复声明 | 数值属于既有稳定协议，本次不为去重修改错误码或扩大领域重构 |
| O7 | 原报告 §5.7 的 Git 故障归因缺少本次独立证据 | 本次仅核对当前仓库与远端状态，不能凭原文确定 stash/工具导致对象清空的根因；保留原记录，不重复宣称因果已证实 |

W6-2 / Mini Program 仍未启动；本次问题修复不构成其开工批准。

## 交付纪律

- 保持主工作区 `main`，不切分支，不强推，不删除用户工作。
- 选号前 `git fetch origin`、`git ls-remote origin refs/heads/main` 核对远端为 `7a94a41ebbf3bb7d9eb06f1e4ea3860a9eecc0fb`；本地 HEAD 与远端一致，现有 V1–V20 连续唯一，追加 V21。
- 本地凭据、`.env`、`.runtime/`、`tools/` 和已有冻结 SHA-256 清单保持原边界，不混入提交。`deploy/postgres/.env.example` 只含示例注释。
- 按本地部署配置、W6-1 实施与复核、底座配置类型修复分组提交。提交并推送源码不代表 V21 已在数据库执行。
