NODE_ENV=production
VITE_APP_TITLE='鲜蔬源商城（预发布）'
# 预发布后端域名待业主提供；小程序需为 HTTPS 且已加入微信公众平台 request 合法域名
VITE_APP_API_URL='https://pre-api.example.com'

# 生产 / 测试构建一律关闭 mock，假数据不得进入产物（vite.config.js 会把 @/mock 别名到空实现）。
VITE_APP_USE_MOCK=false
