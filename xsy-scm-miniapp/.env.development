NODE_ENV=development
VITE_APP_TITLE='鲜蔬源商城（开发）'
VITE_APP_API_URL='http://127.0.0.1:18080'

# 默认开发环境走 mock 契约层，无需后端即可开发（说明见 src/mock/index.js 头部注释）。
# 需要连真实本地后端时用 `npm run dev:h5:api` / `npm run dev:mp-weixin:api`（切到 .env.localhost）。
VITE_APP_USE_MOCK=true
