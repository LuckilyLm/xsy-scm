NODE_ENV=development
VITE_APP_TITLE='鲜蔬源商城（本地联调）'
VITE_APP_API_URL='http://127.0.0.1:18080'

# 本地联调模式：**不走 mock**，直连本机后端。
# 用于后端 /scm/mall/** 就绪后做真实联调；后端未就绪时请用 .env.development（mock）。
VITE_APP_USE_MOCK=false
