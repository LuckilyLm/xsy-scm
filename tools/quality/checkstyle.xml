<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  XSY-SCM Java 静态检查规则（整改计划 §17）。

  只启用低争议硬规则：命名、import、行宽、制表符、文件末尾换行、单一顶层类。
  刻意不启用的：
    - NeedBraces / LeftCurly / RightCurly 等排版规则。存量代码大量使用
      `if (x) throw ...;` 单行写法，启用会产生上万条与命名无关的噪声，
      把真正要治的债务淹掉；排版归属 Spotless，且需要 Q1 迁包后再定形。
    - JavadocMethod / JavadocType / 作者与日期标签。计划 §17 明确不强制，
      为了过检查而生成无价值注释比没有注释更糟。
    - 任何需要 SuppressWithPlainTextCommentFilter 的规则。历史债务由
      tools/quality/quality_guard.py 的 baseline 承接，不用抑制注释散落进源码。

  本配置不使构建失败（maven-checkstyle-plugin failOnViolation=false）：
  它只产出 target/checkstyle-result.xml，由 quality_guard.py 按
  「新增为 0、baseline 只降不升」的棘轮判定。
-->
<module name="Checker">
    <property name="charset" value="UTF-8"/>
    <property name="severity" value="error"/>
    <!-- 换行分隔符接受 lf / cr / crlf：Windows 检出为 CRLF，仓库内为 LF。 -->
    <property name="fileExtensions" value="java"/>

    <module name="NewlineAtEndOfFile">
        <property name="lineSeparator" value="lf_cr_crlf"/>
    </module>

    <module name="FileTabCharacter">
        <property name="eachLine" value="true"/>
    </module>

    <module name="LineLength">
        <property name="max" value="120"/>
        <property name="ignorePattern" value="^package .*|^import .*|^\s*\*.*https?://"/>
    </module>

    <module name="TreeWalker">

        <module name="OuterTypeFilename"/>
        <module name="OneTopLevelClass"/>

        <module name="PackageName">
            <property name="format" value="^[a-z]+(\.[a-z][a-z0-9]*)*$"/>
        </module>
        <module name="TypeName"/>
        <module name="MethodName"/>
        <module name="MemberName"/>
        <module name="ParameterName"/>
        <module name="LocalVariableName"/>
        <module name="LocalFinalVariableName"/>
        <module name="StaticVariableName"/>
        <module name="ConstantName"/>

        <module name="AvoidStarImport"/>
        <module name="RedundantImport"/>
        <module name="UnusedImports">
            <!-- Javadoc 里的 {@link Foo} 算作使用，否则会产生大量误报。 -->
            <property name="processJavadoc" value="true"/>
        </module>

        <module name="ArrayTypeStyle"/>
        <module name="UpperEll"/>
        <module name="ModifierOrder"/>

    </module>
</module>
